---
phase: 02-cockpit-quality-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - transports/api/cockpit.py
  - transports/api/cockpit_entity_routes.py
autonomous: true
requirements:
  - CQG-01
  - CQG-03
  - CQG-04
  - CQG-06

must_haves:
  truths:
    - "All entity/product routes remain reachable at the same /api/umh/entities/* and /api/umh/products/* paths"
    - "cockpit_entity_routes.py exists and imports cleanly"
    - "cockpit.py shrinks by ~280 lines after extraction"
  artifacts:
    - path: "transports/api/cockpit_entity_routes.py"
      provides: "Entity and product routes: portfolio, departments, roles, companies CRUD, products (~10 route handlers)"
      exports: ["configure", "entity_router"]
    - path: "transports/api/cockpit.py"
      provides: "Main router with entity_router mounted"
      contains: "from transports.api import cockpit_entity_routes"
  key_links:
    - from: "transports/api/cockpit.py"
      to: "transports/api/cockpit_entity_routes.py"
      via: "_mount_entity_router() calling configure() then include_router(entity_router)"
      pattern: "cockpit_entity_routes\\.configure"
    - from: "cockpit_entity_routes.py"
      to: "_get_org_id() from cockpit.py"
      via: "configure(get_org_id_fn=...) injection"
      pattern: "_get_org_id\\s*=\\s*get_org_id_fn"
---

<objective>
Extract the entity/product route group (~10 handlers, ~280 lines) from cockpit.py into cockpit_entity_routes.py, following the established cockpit_spine_router.py pattern. cockpit.py shrinks by ~280 lines (from ~3124 to ~2844 lines).

Purpose: Second extraction in Phase 2 — combined with Plan 01 and Plan 03 this brings cockpit.py under 3000 lines.
Output: cockpit_entity_routes.py (new file), cockpit.py (modified, ~2844 lines after this plan)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@transports/api/cockpit.py
@transports/api/cockpit_spine_router.py
@.planning/phases/02-cockpit-quality-gate/02-01-SUMMARY.md
</context>

<interfaces>
<!-- _get_org_id() from cockpit.py — injected via configure() -->
<!-- Lines around 1920-1932 in cockpit.py (before entity routes) -->
<!-- The function is defined somewhere above the entity block and used by all entity handlers -->

Routes to extract (original cockpit.py lines ~1933-2211 after Plan 01 edit, shifted by -418):
After Plan 01 extraction these lines are now approximately 1515-1793.
Use the CURRENT cockpit.py after Plan 01's changes — read it fresh before acting.

Route list (function name → HTTP method → path):
  entity_portfolio              GET   /entities/portfolio
  entity_departments            GET   /entities/departments
  entity_department_detail      GET   /entities/departments/{slug}
  entity_roles                  GET   /entities/roles
  entity_companies              GET   /entities/companies
  entity_company_detail         GET   /entities/companies/{company_id}
  upsert_company                POST  /entities/companies          (no operator_auth)
  product_connections           GET   /products
  refresh_product_connections   POST  /products/refresh            (no operator_auth)

Note: upsert_company and refresh_product_connections have no operator-auth in the original.
Keep this exactly — do not add auth that wasn't there.

_get_org_id() helper is used by all entity_* handlers — inject via configure().
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create cockpit_entity_routes.py</name>
  <files>transports/api/cockpit_entity_routes.py</files>

  <read_first>
    - transports/api/cockpit_spine_router.py (lines 1-80 — established pattern)
    - transports/api/cockpit.py (entity routes block: grep -n "entity_portfolio\|entity_departments\|entity_companies\|products" to find current lines after Plan 01 edit)
  </read_first>

  <action>
Create `transports/api/cockpit_entity_routes.py` using the cockpit_spine_router.py pattern.

Module docstring: "Cockpit entity and product routes — portfolio, departments, roles, companies CRUD, product connections. Extracted from cockpit.py (Phase 10.0). Auth model: configure() must be called before include_router(). Receives operator-auth dependency and _get_org_id from cockpit.py."

Imports:
```python
from __future__ import annotations
import logging
from typing import Any, Callable
from fastapi import APIRouter, Depends
```

Module-level state:
```python
entity_router: APIRouter = APIRouter()
_get_org_id: Callable[[], str] = lambda: ""
_configured: bool = False
```

configure() signature — note: no _check_rate_limit needed (no rate-limited routes):
```python
def configure(
    get_org_id_fn: Callable[[], str],
    require_operator_dep: Any,
) -> None:
    global _get_org_id, _configured, entity_router
    _get_org_id = get_org_id_fn
    _configured = True
    entity_router = _build_router(require_operator_dep)
```

_build_router() with route registration:

Read-only routes (no auth):
  r.add_api_route("/entities/portfolio", _entity_portfolio, methods=["GET"])
  r.add_api_route("/entities/departments", _entity_departments, methods=["GET"])
  r.add_api_route("/entities/departments/{slug}", _entity_department_detail, methods=["GET"])
  r.add_api_route("/entities/roles", _entity_roles, methods=["GET"])
  r.add_api_route("/entities/companies", _entity_companies, methods=["GET"])
  r.add_api_route("/entities/companies/{company_id}", _entity_company_detail, methods=["GET"])
  r.add_api_route("/entities/companies", _upsert_company, methods=["POST"])
  r.add_api_route("/products", _product_connections, methods=["GET"])
  r.add_api_route("/products/refresh", _refresh_product_connections, methods=["POST"])

No privileged routes in this module (upsert_company and refresh_product_connections have no operator_auth in original cockpit.py).

Handler functions prefixed with underscore: entity_portfolio → _entity_portfolio, etc.
Copy implementation bodies verbatim from cockpit.py.
All inline imports (from projections.eos.entities import ..., from substrate.state.stores.entity_store import ...) stay inline.
The `_get_org_id()` calls in handlers use the module-level `_get_org_id`.
  </action>

  <verify>
    <automated>cd /opt/OS/.claude/worktrees/phase10-0-template-library && python3 -m py_compile transports/api/cockpit_entity_routes.py && echo "compile ok"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -m py_compile transports/api/cockpit_entity_routes.py` exits 0
    - `grep -c "add_api_route" transports/api/cockpit_entity_routes.py` returns 9
    - File contains `def configure(` with `get_org_id_fn` parameter
    - File contains `entity_router: APIRouter = APIRouter()`
    - File does NOT contain `Depends(_require_operator_role)` on any route (none in original)
    - File does NOT import from `transports.api.cockpit`
  </acceptance_criteria>

  <done>cockpit_entity_routes.py compiles clean, 9 route handlers, configure() with get_org_id_fn, no unauthorized auth additions</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire cockpit_entity_routes.py into cockpit.py and delete extracted routes</name>
  <files>transports/api/cockpit.py</files>

  <read_first>
    - transports/api/cockpit.py (current state after Plan 01 — read it fresh: find entity routes with grep -n "entity_portfolio\|upsert_company\|product_connections\|_get_org_id" to confirm current line numbers)
    - transports/api/cockpit_entity_routes.py (just created — verify configure() signature)
  </read_first>

  <action>
Edit `transports/api/cockpit.py` in two steps:

STEP 1 — Delete the extracted route block:
Find the entity/product block by searching for:
  - Start: the `# ── Entity views (Portfolio / Company / Department / Role) ───` comment
  - End: the closing of `refresh_product_connections` function body
  - This is the block containing: entity_portfolio, entity_departments, entity_department_detail, entity_roles, entity_companies, entity_company_detail, upsert_company, product_connections, refresh_product_connections

Use grep to find exact current line numbers after Plan 01 edit:
  grep -n "Entity views\|def entity_portfolio\|def refresh_product_connections\|Products.*EOS.*CreatorOS" transports/api/cockpit.py

Delete from the "Entity views" comment line through the end of refresh_product_connections.
Do NOT delete _get_org_id() definition — it must remain in cockpit.py (it is also used by eos_pipeline, eos_kpis, eos_activity handlers above this block).

STEP 2 — Add _mount_entity_router() and call it:
Append after the existing `_mount_organism_router()` call at the bottom of the file:

```python
def _mount_entity_router() -> None:
    from transports.api import cockpit_entity_routes
    cockpit_entity_routes.configure(
        get_org_id_fn=_get_org_id,
        require_operator_dep=_require_operator_role,
    )
    router.include_router(cockpit_entity_routes.entity_router)


_mount_entity_router()
```

Verify cockpit.py line count after edit is approximately 2844 (3124 - 280 = 2844).
  </action>

  <verify>
    <automated>cd /opt/OS/.claude/worktrees/phase10-0-template-library && python3 -m py_compile transports/api/cockpit.py && echo "compile ok" && wc -l transports/api/cockpit.py</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -m py_compile transports/api/cockpit.py` exits 0
    - `wc -l transports/api/cockpit.py` reports between 2780 and 2920 (was ~3124, extracted ~280 lines)
    - `grep "def entity_portfolio" transports/api/cockpit.py` returns nothing (removed)
    - `grep "def upsert_company" transports/api/cockpit.py` returns nothing (removed)
    - `grep "def _get_org_id" transports/api/cockpit.py` returns the definition (NOT removed)
    - `grep "_mount_entity_router" transports/api/cockpit.py` returns def and call
  </acceptance_criteria>

  <done>cockpit.py compiles clean, entity/product routes removed, _get_org_id preserved, _mount_entity_router wired, line count ~2844</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /opt/OS/.claude/worktrees/phase10-0-template-library
python3 -m py_compile transports/api/cockpit.py
python3 -m py_compile transports/api/cockpit_entity_routes.py
wc -l transports/api/cockpit.py
grep -c "add_api_route" transports/api/cockpit_entity_routes.py
```

Expected: both compile clean, cockpit.py ~2844 lines, 9 route registrations.
</verification>

<success_criteria>
1. cockpit_entity_routes.py exists, compiles clean, has configure() + 9 route handlers
2. cockpit.py compiles clean and is ~2844 lines (down from ~3124)
3. All entity and product routes preserved at /api/umh/entities/* and /api/umh/products/* paths
4. No unauthorized auth changes — upsert_company and refresh_product_connections remain auth-free
5. _get_org_id() remains in cockpit.py
</success_criteria>

<output>
After completion, create `.planning/phases/02-cockpit-quality-gate/02-02-SUMMARY.md`
</output>
