---
phase: 01-preflight
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [data/audits/2026-05-29_phase10_0_preflight_audit.md]
autonomous: true
requirements: [PRE-03, PRE-04]
must_haves:
  truths:
    - "Cadence scheduler reports dry_run_only mode when queried via cockpit API"
    - "All four production truth endpoints return 200 or expected auth response post-merge"
    - "Preflight audit document exists at data/audits/2026-05-29_phase10_0_preflight_audit.md"
    - "No runtime mutation occurred during any verification step"
  artifacts:
    - path: "data/audits/2026-05-29_phase10_0_preflight_audit.md"
      provides: "Complete preflight verification evidence"
      contains: "dry_run_only"
  key_links:
    - from: "GET /api/umh/organism/autonomous-cadence"
      to: "cadence mode field"
      via: "curl with operator token"
      pattern: "dry_run_only"
    - from: "GET /api/umh/organism/autonomous-pr-factory/production-truth"
      to: "HTTP 200 response"
      via: "curl with operator token"
      pattern: "200"
---

<objective>
Verify cadence mode is dry_run_only and all four production truth endpoints are operational post-merge. Write the preflight audit document capturing all verification evidence.

Purpose: Prove the system is in a known-good state before Phase 10.0 build work begins.
Output: Confirmed cadence mode, confirmed endpoint health, audit document at data/audits/2026-05-29_phase10_0_preflight_audit.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-preflight/01-01-SUMMARY.md

Cockpit base URL: https://universalmetaharness.tech
API key header: X-API-Key (value from /opt/OS/services/.env: UMH_OPERATOR_API_KEY)
Operator token header: X-Operator-Token (value from /opt/OS/services/.env: UMH_OPERATOR_TOKEN)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify cadence mode is dry_run_only</name>
  <files></files>
  <read_first>
    Read /opt/OS/services/.env to get UMH_OPERATOR_TOKEN and UMH_OPERATOR_API_KEY values.
    Do NOT print these values in output — use them in curl headers only.
  </read_first>
  <action>
    Query the cadence status endpoint and confirm mode is dry_run_only.

    Command (substitute actual token from services/.env):
    curl -s -H "X-Operator-Token: ${UMH_OPERATOR_TOKEN}" \
      https://universalmetaharness.tech/api/umh/organism/autonomous-cadence

    Parse the JSON response. Verify:
    - Response HTTP status is 200
    - JSON contains "mode": "dry_run_only"
    - JSON contains "no_auto_merge": true (or equivalent policy field)
    - JSON contains a last_run or total_runs field showing previous dry-run activity

    Also check the policy subobject if present:
    - max_dry_runs_per_day should be 24
    - require_operator_enable_for_pr_creation should be true

    Expected pre-known state from phase9_9_cadence_dry_run_activation.json:
    - mode: dry_run_only
    - dry_runs_today: may have incremented since phase 9.9
    - total_runs: >= 1
    - prs_today: 0
    - no_auto_merge: true

    Record the full JSON response for the audit document.
    If mode is NOT "dry_run_only": this is a blocker — stop and report immediately.
  </action>
  <verify>
    <automated>curl -s -H "X-Operator-Token: $(grep UMH_OPERATOR_TOKEN /opt/OS/services/.env | cut -d= -f2)" https://universalmetaharness.tech/api/umh/organism/autonomous-cadence | python3 -c "import sys,json; d=json.load(sys.stdin); assert d.get('mode') == 'dry_run_only' or d.get('policy', {}).get('mode') == 'dry_run_only', f'mode mismatch: {d}'; print('CADENCE_DRY_RUN_ONLY')"</automated>
  </verify>
  <done>
    - Cadence endpoint returns 200
    - mode field (top-level or in policy) is exactly "dry_run_only"
    - Full JSON response captured for audit
    - No mutation fields show any activity (prs_today: 0, no_auto_merge: true)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify four production truth endpoints operational post-merge</name>
  <files></files>
  <read_first>
    Read /opt/OS/services/.env for UMH_OPERATOR_TOKEN and UMH_OPERATOR_API_KEY.
    Reference /opt/OS/data/umh/autonomous_lane/phase9_9_live_api_verification.json for the expected endpoint list.
  </read_first>
  <action>
    The four production truth endpoints are (per phase 9.9 audit):
    1. GET /api/umh/organism/autonomous-pr-factory/production-truth  (requires operator token)
    2. GET /api/umh/organism/autonomous-pr-factory/merge-verifications  (requires operator token)
    3. GET /api/umh/organism/autonomous-cadence  (requires operator token, verified in Task 1)
    4. GET /api/umh/build  (requires api key)

    Run each curl and record HTTP status + truncated response:

    ENDPOINT_1:
    curl -s -o /tmp/pt1.json -w "%{http_code}" \
      -H "X-Operator-Token: ${UMH_OPERATOR_TOKEN}" \
      https://universalmetaharness.tech/api/umh/organism/autonomous-pr-factory/production-truth

    ENDPOINT_2:
    curl -s -o /tmp/pt2.json -w "%{http_code}" \
      -H "X-Operator-Token: ${UMH_OPERATOR_TOKEN}" \
      https://universalmetaharness.tech/api/umh/organism/autonomous-pr-factory/merge-verifications

    ENDPOINT_3: Already verified in Task 1.

    ENDPOINT_4:
    curl -s -o /tmp/pt4.json -w "%{http_code}" \
      -H "X-API-Key: ${UMH_OPERATOR_API_KEY}" \
      https://universalmetaharness.tech/api/umh/build

    For each endpoint record:
    - HTTP status (expected: 200)
    - First 200 chars of JSON response
    - Any error message if not 200

    For /build endpoint specifically: extract the commit SHA from the response JSON.
    It should match main HEAD (94480e88). Command:
    cat /tmp/pt4.json | python3 -c "import sys,json; d=json.load(sys.stdin); print('build_commit:', d.get('commit', d.get('sha', 'NOT_FOUND')))"

    Acceptance: all four endpoints return 200 and build commit matches 94480e88.
    If build commit does not match: record the divergence but do not block — the cockpit may
    need a redeploy, which is a separate concern outside Phase 1 scope.
  </action>
  <verify>
    <automated>curl -s -w "%{http_code}" -H "X-API-Key: $(grep UMH_OPERATOR_API_KEY /opt/OS/services/.env | cut -d= -f2)" https://universalmetaharness.tech/api/umh/build | tail -3 | grep -E "^200$"</automated>
  </verify>
  <done>
    - All four production truth endpoints return HTTP 200
    - /build response captured and commit SHA extracted
    - Endpoint responses recorded (truncated) for audit document
    - Any commit divergence noted (non-blocking unless endpoints are down)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write preflight audit document</name>
  <files>data/audits/2026-05-29_phase10_0_preflight_audit.md</files>
  <read_first>
    Read .planning/phases/01-preflight/01-01-SUMMARY.md for PR merge verification results.
    Collect all outputs from Task 1 and Task 2 in this plan.
  </read_first>
  <action>
    Create /opt/OS/data/audits/2026-05-29_phase10_0_preflight_audit.md.

    The document must contain these sections:

    # Phase 10.0 Preflight Audit
    **Date:** 2026-05-29
    **Auditor:** Phase 1 executor
    **Phase:** 10.0 / Plan 01-02

    ## 1. PR #44 Merge Verification (PRE-01)
    - PR number: 44
    - Title: "feat: phase 9.9 — production truth deployment verification + scheduled cadence activation"
    - State: MERGED
    - Merge commit: 1a17dfb8
    - Merged at: [timestamp from gh output]
    - Base branch: main
    - Head branch: worktree-phase9-9-deployment-verification
    - Files changed: 7
    - File classification table:
      | File | Type | Runtime Impact |
      |------|------|----------------|
      | data/umh/autonomous_lane/phase9_9_browser_smoke_test.json | DATA_ARTIFACT | None |
      | data/umh/autonomous_lane/phase9_9_cadence_dry_run_activation.json | DATA_ARTIFACT | None |
      | data/umh/autonomous_lane/phase9_9_cleanup_report.json | DATA_ARTIFACT | None |
      | data/umh/autonomous_lane/phase9_9_live_api_verification.json | DATA_ARTIFACT | None |
      | data/umh/autonomous_lane/phase9_9_pr43_merge_verification.json | DATA_ARTIFACT | None |
      | data/umh/autonomous_lane/phase9_9_pr43_production_truth_verification.json | DATA_ARTIFACT | None |
      | docs/audits/convergence/phase9_9_production_truth_deployment_and_scheduled_cadence.md | DOCS | None |
    - Runtime code pollution: NONE

    ## 2. Runtime Commit Alignment (PRE-02)
    - Main HEAD: 94480e88
    - VPS HEAD: [observed from 01-01]
    - Worktree HEAD: [observed from 01-01]
    - Alignment: [MATCH / DIVERGENCE + explanation]

    ## 3. Cadence Mode Verification (PRE-03)
    - Endpoint: GET /api/umh/organism/autonomous-cadence
    - HTTP Status: [observed]
    - Mode: [observed — must be dry_run_only]
    - Policy fields:
      - max_dry_runs_per_day: [observed]
      - require_operator_enable_for_pr_creation: [observed]
      - no_auto_merge: [observed]
    - Full response: [paste JSON]
    - Result: PASS / FAIL

    ## 4. Production Truth Endpoints (PRE-04)
    | Endpoint | HTTP Status | Notes |
    |----------|-------------|-------|
    | GET /organism/autonomous-pr-factory/production-truth | [status] | [first 100 chars] |
    | GET /organism/autonomous-pr-factory/merge-verifications | [status] | [first 100 chars] |
    | GET /organism/autonomous-cadence | [status] | verified in PRE-03 |
    | GET /build | [status] | commit=[sha] |

    - Build commit SHA: [observed]
    - Expected main HEAD: 94480e88
    - Commit alignment: [MATCH / DIVERGENCE]
    - Result: PASS / FAIL

    ## 5. Overall Preflight Decision
    - PRE-01: [PASS/FAIL]
    - PRE-02: [PASS/FAIL]
    - PRE-03: [PASS/FAIL]
    - PRE-04: [PASS/FAIL]
    - **DECISION: [CLEAR TO PROCEED / BLOCKED]**
    - Blockers (if any): [list or NONE]

    Fill every section with actual observed values from Tasks 1 and 2.
    Do not leave placeholders in the final document.
  </action>
  <verify>
    <automated>ls /opt/OS/data/audits/2026-05-29_phase10_0_preflight_audit.md && grep -c "dry_run_only" /opt/OS/data/audits/2026-05-29_phase10_0_preflight_audit.md | grep -v "^0$"</automated>
  </verify>
  <done>
    - File exists at /opt/OS/data/audits/2026-05-29_phase10_0_preflight_audit.md
    - Contains all 5 sections with real observed values (no placeholders)
    - Contains "dry_run_only" at least once
    - Overall decision is clearly stated as CLEAR TO PROCEED or BLOCKED
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
- Cadence endpoint returns `dry_run_only` confirmed by automated grep
- All 4 production truth endpoints return HTTP 200
- Audit document exists with zero placeholders and a clear decision
</verification>

<success_criteria>
PRE-03: Cadence mode confirmed dry_run_only via live API call.
PRE-04: All four production truth endpoints return HTTP 200 post-merge.
Audit document at data/audits/2026-05-29_phase10_0_preflight_audit.md contains full evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-preflight/01-02-SUMMARY.md` with:
- cadence_mode: dry_run_only (or observed value)
- endpoint_1_status: (HTTP code)
- endpoint_2_status: (HTTP code)
- endpoint_3_status: (HTTP code)
- endpoint_4_status: (HTTP code)
- build_commit: (observed SHA)
- preflight_decision: CLEAR_TO_PROCEED or BLOCKED
- audit_path: data/audits/2026-05-29_phase10_0_preflight_audit.md
</output>
