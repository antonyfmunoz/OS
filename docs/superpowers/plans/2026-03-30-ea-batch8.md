# EA System — Batch 8 Build Plan
**Date:** 2026-03-30  
**Branch:** feature/ea-system  
**Project:** /opt/OS  

## Context
EntrepreneurOS EA system — adding intelligence and tracking capabilities.
All Python. All files under /opt/OS. Services run in Docker.
DB: Neon via `eos_ai.db.get_conn(ctx.org_id)`.
LLM: `eos_ai.model_router.get_router()` / `TaskType`.
Standard pattern: wrap everything in try/except, log warnings, never crash.

**Key note:** `nightly_maintenance.sh` does NOT exist — Part 2 wires into
knowledge files only (no shell script to modify).

---

## Task 1 — Morning intelligence brief (PART 1)

**Files to create:**
- `/opt/OS/scripts/morning_intel.py`

**Files to modify:**
- crontab (add 5:45am entry)

**Spec:** Create `/opt/OS/scripts/morning_intel.py` exactly as specified:
- Pulls overnight signals from Neon (event_types: icp_signal, market_signal, research_output, intel_report, last 24h)
- Reads most recent 3 knowledge files from 07_Knowledge/Reports/Market_Reports/*.md and 07_Knowledge/ICP/*.md
- Gets portfolio context via PortfolioAgent (scan_all_ventures + identify_binding_constraint)
- Synthesizes with LLM (TaskType.ANALYSIS) — under 200 words, high signal only
- Posts to Discord GENERAL_CHANNEL_ID=1486289444830056540 via discord.Client
- Add crontab entry: `45 5 * * * cd /opt/OS && python3 scripts/morning_intel.py >> /opt/OS/logs/morning_intel.log 2>&1`

**Verify:** `python3 -c "from scripts.morning_intel import build_intel_brief; print('ok')"`

---

## Task 2 — Competitive intelligence module (PART 2)

**Files to create:**
- `/opt/OS/eos_ai/competitive_intel.py`

**Files to modify:**
- `/opt/OS/13_Scripts/discord_bot.py` — add `!competitive` command

**Spec:** Create `/opt/OS/eos_ai/competitive_intel.py` exactly as specified:
- `COMPETITORS` dict with lyfe_institute, empyrean_creative, personal_brand keys
- `log_competitor_signal(venture, competitor, signal, implication, ctx)` → writes competitor_signal event to Neon
- `get_recent_signals(venture, days, ctx)` → queries Neon for competitor_signal events
- `synthesize_competitive_landscape(venture, ctx)` → LLM synthesis (TaskType.ANALYSIS)

Discord command `!competitive [venture]`:
- Find where `!audit` command is in discord_bot.py — add `!competitive` before it
- Default venture = empyrean_creative
- Calls synthesize_competitive_landscape, sends result

**Note:** nightly_maintenance.sh doesn't exist — skip that wiring step entirely.

**Verify:** `python3 -c "from eos_ai.competitive_intel import synthesize_competitive_landscape; print('ok')"`

---

## Task 3 — Relationship health scoring (PART 3)

**Files to modify:**
- `/opt/OS/eos_ai/person_recognition.py` — add `score_relationship_health()`
- `/opt/OS/13_Scripts/discord_bot.py` — add `!relationship` command
- `/opt/OS/scripts/relationship_nurture.py` — use `score_relationship_health()` to sort by health ascending

**Spec for `score_relationship_health(name, email, ctx)`:**
- Add after existing functions in person_recognition.py (before end of file)
- Queries Notion meetings DB (NOTION_API_KEY, NOTION_MEETINGS_ID env vars)
- Filters by name OR email using `{'or': [...]}` filter
- Scores: base 0.5, adjusts for days_since_contact, total_meetings, no_shows, completed
- Returns: score(0-1), status(Strong/Active/At risk/Cold), days_since_contact, total_meetings, completed_meetings, no_shows, factors[], last_contact

**Spec for relationship_nurture.py:**
- Import `score_relationship_health` from `eos_ai.person_recognition`  
- After collecting `cold_contacts`, call `score_relationship_health` for each
- Sort cold_contacts by score ascending (coldest/lowest health first)
- Include health score in the Discord message per contact

**Discord `!relationship [name]`:**
- Read discord_bot.py, find where `!nurture` command is, add `!relationship` before it
- Calls score_relationship_health + build_intelligence_profile + format_intelligence_profile
- Renders health bar: `█` * int(score*10) + `░` * remaining

**Verify:** `python3 -c "from eos_ai.person_recognition import score_relationship_health; print('ok')"`

---

## Task 4 — Meeting ROI tracking (PART 4)

**Files to modify:**
- `/opt/OS/eos_ai/meetings.py` — add `calculate_meeting_roi()`
- `/opt/OS/13_Scripts/discord_bot.py` — add `!meetingroi` command
- `/opt/OS/scripts/weekly_review.py` — add meeting ROI section after portfolio health

**Spec for `calculate_meeting_roi(venture, days, ctx)`:**
- Add after `draft_meeting_agenda()` at end of meetings.py
- Queries Notion meetings DB filtered by date cutoff (and optionally venture)
- Counts: total, completed, no_show, cancelled
- Tracks by_type: {total, completed, advanced} — advanced = outcomes contain keywords
- Advancement keywords: closed, proposal, next step, moving forward, agreed, yes, signed
- Returns: total, completed, no_show, cancelled, by_type, conversion_rate, top_converting_type

**weekly_review.py:** After the portfolio health block (after `sections.append('')` at line ~124), add meeting ROI section using `calculate_meeting_roi(days=7)`.

**Discord `!meetingroi [venture]`:**
- Find where `!meetingroi` would fit near other meeting commands in discord_bot.py
- Add before `!competitive` or near `!outcome` command
- Optional venture arg; shows 30-day stats + by_type breakdown

**Verify:** `python3 -c "from eos_ai.meetings import calculate_meeting_roi; print('ok')"`

---

## Task 5 — Stakeholder map (PART 5)

**Files to create:**
- `/opt/OS/eos_ai/stakeholder_map.py`

**Files to modify:**
- `/opt/OS/13_Scripts/discord_bot.py` — add `!stakeholders` and `!add_stakeholder` commands

**Spec:** Create `/opt/OS/eos_ai/stakeholder_map.py` exactly as specified:
- `add_stakeholder(name, venture, role, influence, status, notes, email, ctx)` → writes stakeholder event to Neon
- `get_stakeholders(venture, ctx)` → deduplicates by name+venture, returns list
- `generate_stakeholder_brief(venture, ctx)` → calls score_relationship_health per stakeholder, LLM synthesis

**Discord `!stakeholders [venture]` and `!add_stakeholder [name] | [venture] | [role] | [influence]`:**
- Add near the bottom of discord_bot.py command block, before the calendar section
- `!stakeholders` without arg shows all; with venture arg shows brief + list
- `!add_stakeholder` parses pipe-separated parts

**Verify:** `python3 -c "from eos_ai.stakeholder_map import get_stakeholders, add_stakeholder; print('ok')"`

---

## Task 6 — Document filing system (PART 6)

**Files to create:**
- `/opt/OS/eos_ai/document_filer.py`

**Files to modify:**
- `/opt/OS/eos_ai/email_gps.py` — wire attachment processing into `process_inbox()`
- `/opt/OS/13_Scripts/discord_bot.py` — add `!documents` command

**Spec for `document_filer.py`:** Create exactly as specified:
- `FILING_STRUCTURE` dict mapping doc types to folder paths
- `classify_document(filename, subject, sender)` → LLM classification, returns type+folder+venture+priority+requires_review+confidence
- `log_document(filename, doc_type, folder, venture, sender, requires_review, ctx)` → writes document_filed event to Neon
- `process_email_attachments(subject, sender, attachment_names, ctx)` → classify+log each, return results list

**email_gps.py wiring:**
- In `process_inbox()`, after `self.apply_label_to_email(email.id, ...)` call (line ~807), add attachment processing block exactly as specified
- Uses `email.subject`, `email.from_address` — these are available as ProcessedEmail attributes
- Variable names in scope: `email` (ProcessedEmail), `raw` (dict from gws)
- Get payload from `raw` dict (not email object): `raw.get('payload', {})`

**Discord `!documents`:**
- Add near `!audit` command
- Queries Neon for document_filed events, last 30 days, limit 10

**Verify:** `python3 -c "from eos_ai.document_filer import classify_document, process_email_attachments; print('ok')"`

---

## Final verification (after all tasks)

```python
python3 -c "
import sys; sys.path.insert(0, '/opt/OS')
from scripts.morning_intel import build_intel_brief
from eos_ai.competitive_intel import synthesize_competitive_landscape
from eos_ai.person_recognition import score_relationship_health
from eos_ai.meetings import calculate_meeting_roi
from eos_ai.stakeholder_map import get_stakeholders, add_stakeholder
from eos_ai.document_filer import classify_document, process_email_attachments
print('all imports clean')

health = score_relationship_health(name='Juan Munoz')
print(f'Juan Munoz health: {health[\"score\"]} — {health[\"status\"]}')

roi = calculate_meeting_roi(days=30)
print(f'Meeting ROI: {roi}')
"

docker compose restart os-discord os-webhook
```
