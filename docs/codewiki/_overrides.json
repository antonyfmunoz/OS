{
  "substrate/types.py": "Canonical domain type system — SignalEnvelope, RiskClass, Modality and 30+ Pydantic models shared by every layer",
  "substrate/control_plane/agents/agent_hierarchy.py": "Agent hierarchy — formal authority structure from founder down; resolves venture display names from instance config",
  "substrate/governance/policy/authority_engine.py": "Authority engine — maps actions to RiskClass tiers and enforces approval authority per risk level",
  "substrate/state/business/venture_knowledge.py": "VentureKnowledgeBase — loads venture profiles from instance JSON; resolves venture names and metadata",
  "substrate/state/context/context.py": "SubstrateContext — org/venture runtime context loaded from UMH_ORG_ID/UMH_USER_ID env",
  "substrate/state/registries/skill_registry.py": "SkillRegistry — per-org skill catalog with load/reset accessors",
  "substrate/state/session/session_state.py": "SessionState — persistent session key/value state backed by session_state.json",
  "substrate/composition/mastery/authoring/__main__.py": "python -m entrypoint for the mastery authoring CLI",
  "substrate/composition/mastery/research/__main__.py": "python -m entrypoint for the mastery research CLI",
  "scripts/cron-run": "Cron wrapper — load-gated (2.0/core), nice+flock protected runner for scheduled scripts (CPU Gate layer 4)",
  "scripts/update-graph": "End-to-end knowledge refresh: codebase_graph.py → build_palace.py → summarize_nodes.py",
  "scripts/pre-commit": "Pre-commit gate runner — executes the repo's enforcement gate scripts",
  "scripts/watch-cognition": "Watcher wrapper for cognition/graph freshness monitoring",
  "scripts/claude-cpu-limited": "cpulimit wrapper for running Claude CLI under a CPU ceiling",
  "scripts/agent_executor.log": "runtime log of agent executor runs (should live in logs/)",
  "scripts/notion_tasks_sync_state.json": "sync-state checkpoint for Notion tasks sync",
  "scripts/.env.beast.tpl": "1Password op-run env template for Beast node credentials",
  ".mcp.json": "MCP server registration for Claude Code sessions",
  ".claude/settings.json": "Claude Code project settings (model pin, hooks, permissions)",
  ".claude/settings.local.json": "Claude Code machine-local settings overrides",
  ".claude/last_cc_version": "marker file — last seen Claude Code CLI version",
  ".claude/scheduled_tasks.lock": "lockfile guarding scheduled-task single-run semantics",
  ".planning/config.json": "GSD planning workspace configuration",
  ".vscode/settings.json": "VS Code workspace settings"
}
