# Cockpit Surface Convergence Ledger

Started 2026-07-21 (MVP Wave 1). Every known divergence from the Convergence
Law (`.claude/rules/convergence-law.md`) is recorded here with an owner
adjudication and a migration path. Entries are RESOLVED or RETIRED — never
silently deleted. New divergence may not be added to code without a ledger
entry; the ledger is the license for bounded compatibility debt, not a
graveyard.

Status values: `ACTIVE_DEBT` (bounded compatibility, retirement scheduled) |
`ADJUDICATED` (naming/ownership ruling recorded, no code debt) |
`RESOLVED` (converged; entry kept for history).

## Concept-level entries

| # | Divergence | Canonical owner | Status | Migration path |
|---|---|---|---|---|
| 1 | `ObjectiveQueue` (`objective_queue.py`) holds objective-like items with its own lifecycle | `GoalRegistry` Goal(OBJECTIVE) | ACTIVE_DEBT | Zero new Wave 1 writes; read adapters only; migrate in execution-convergence wave |
| 2 | `Coordinator.Objective` + `WorkUnit` (coordinator.py) — legacy decomposition vocabulary | Goal(OBJECTIVE) + WorkPacket | ACTIVE_DEBT | Same as #1 |
| 3 | IntentLoop records (`operator/intent_loop/`) were the work-capture truth for Cockpit submissions | Intent protocol → GoalRegistry / PlanningStore / packet store | ACTIVE_DEBT (chat path RESOLVED) | C4 cutover DONE for the Cockpit converse path (`try_chat_planning_rail` replaced the legacy rail; spy tests prove zero legacy mutation calls, §23.5). Remaining debt: explicit `POST /intent-loop/submit` + panel read surface stay as bounded legacy compatibility until the execution-convergence wave |
| 4 | `WorkLineage` name collision: continuity aggregate (`continuity_runtime.WorkLineage`) vs per-Task planning lineage | Both legitimate, DIFFERENT concepts | ADJUDICATED | New contract named `WorkLineageContext` (`substrate/contracts/work_context.py`); no shadow, no LEGACY_DUPLICATES growth |
| 5 | Planning-time gap artifact vs strategic `Gap` (strategic_gap_engine) | `Gap` = canonical strategic gap | ADJUDICATED | Planning artifact classified GapAssessmentSnapshot — evidence class, non-authoritative, goal_refs link to canonical Gaps (C3 rename of `GapModel`) |
| 6 | StrategicGapEngine gaps/recommendations/decisions still write under tracked `data/umh/strategic_gaps/` | runtime-state boundary | ACTIVE_DEBT | Goal store migrated (§22.1, Wave 1). Remaining sub-stores migrate with the strategic-engine convergence wave; no new Wave 1 write paths added to them |
| 7 | Per-store private fcntl lock idiom repeated (planning store, approval_store, settings_persistence, GoalRegistry, …) | one canonical lock utility (future) | ACTIVE_DEBT | Extract shared helper when a 6th copy would appear; do not fork the idiom further |
| 8 | SelfBuild / BuildLoop / Actions stores hold build work outside canonical packets | WorkPacket + WorkGraph | ACTIVE_DEBT | Verified read adapters only (visually distinct on Work board); store migration scheduled post-Wave-1 |
| 9 | Seat/session-derived identity in legacy auth paths | PrincipalContext (principal ≠ tenant ≠ membership ≠ seat) | ACTIVE_DEBT | `substrate/contracts/principal_resolution.py` derives legacy_derived ids deterministically; membership NEVER from a browser session; native issuance in tenancy wave |
| 10 | Per-route/per-module `EventSpine()` constructions (cockpit routes, operator engines) split event truth | `get_shared_event_spine()` (event_spine.py — persisted, recovered) | ACTIVE_DEBT | Planning path uses ONLY the shared spine (§22.6, enforced by test AC); per-route instances migrate in the observability convergence wave |
| 11 | `instruction_compilation.compile_instruction_package` has no production caller yet (Wave 1 planning is deterministic-only) | the §9 seam itself | RESOLVED (Wave 2) | First production consumer landed: `substrate/execution/attempts/dispatch.py::compile_attempt_package` seals one ModelExecutionPackage per attempt (Wave 2 C3). No raw prompt strings on the execution path |
| 12 | Legacy execution stores (coordinator `CoordinatorExecutionPlan` JSON + `LifecycleEvent` JSONL; `ExecutionReceipt`) | `ExecutionAttempt` ledger + shared EventSpine | ACTIVE_DEBT | Zero new Wave 2 writes from the canonical execution path (attempts/* imports none of them — enforced by `test_wave2_convergence_gates`). Coordinator plan/lifecycle stores are read-only compat; retire in a later wave |
| 13 | `ExecutionGraph` defined twice — `plan_execution_adapter.py:269` (ExecutablePlan DAG) vs `execution_graph.py` (ExecutionGraphNode DAG) | `execution_graph.ExecutionGraph` canonical | ADJUDICATED | Distinct concerns; adapter-local one registered as a Wave 2 legacy-duplicate homonym in `canonical_types.LEGACY_DUPLICATES_META` (shrink-only, sunset 2026-12-31), retired when the adapter leaves the legacy path |
| 14 | Execution-readiness had TWO representations and ZERO enforcement: `readiness.evaluate_execution_readiness` defined 15 fail-closed checks with **no production caller**, while the scheduler open-coded a partial subset inline. `grant.role_ids`, `grant.allowed_tools`, `grant.cost_limit_usd`, `work_scope.target_kind`, prohibited skills and rollback obligations were therefore unenforced at admission — operator-set bounds that imposed nothing. Comments in `lifecycle.py` ("requires AUTHORIZED readiness", "authorization re-validated at that instant") and `placement.py` ("tools already validated ... in readiness") asserted guarantees no code provided | `admission.authorize_admission` — the ONE canonical fail-closed admission authority | RESOLVED (Wave 2 R3) | `substrate/execution/attempts/admission.py` decides all 17 admission conditions and is consumed ATOMICALLY by `AttemptScheduler._admit` under the scheduler lock, on the re-read packet and re-read grant, immediately before the lease. `ExecutionReadinessAssessment` is retained as the PRE-GRANT advisory assessment surface only — it is not an enforcement rival and holds no admission authority. The three false comments are corrected in the same change. 12/12 mutations killed (each guard independently load-bearing) |
| 15 | Two independently-authored TOOL vocabularies with no mapping: `packet.required_tools` is PLANNING vocabulary copied from the archetype `tool_policy` (`repository`, `typecheck`, `editor`, `shell_gated`, `docker`, `crm`, `search`, `read`, `graph_query`, `test_runner`), while `RoleContract.allowed_tools` is a separate vocabulary (`code_edit`, `test_runner`, `deploy`, `web_search`, `document_analysis`, `monitor`, `config`). They share exactly ONE token out of sixteen. Comparing them at admission refused 5 of 5 real archetypes — every packet the canonical compiler produces (adversarial review R4-3) | ONE tool vocabulary shared by archetypes and RoleContracts (owner: planning archetypes + `substrate/organism/role_contracts.py`) | ACTIVE_DEBT | Admission check 10 deliberately enforces ONLY `grant.allowed_tools` — a real operator bound expressed in the Task's own vocabulary — and does NOT compare the role leg; the exclusion is pinned by `test_role_tool_vocabulary_is_not_compared_against_task_tools` so it cannot be silently "fixed" by re-adding a comparison that refuses all real work. Reachability: the role leg is unenforced at admission today. Residual risk: a Task may require a tool its ROLE would not permit. Compensating controls, stated at their REAL strength: (a) the operator's `grant.allowed_tools` bound — ENFORCED strictly by admission check 10, but only when the operator declares one (`request_execution_authorization` defaults it to `[]`, so it is absent on the default path); (b) fail-closed diff-scope verification — `verification.py::_diff_scope_verdict` refuses any attempt whose diff touches outside the Task's DECLARED writable scope, which bounds what an unauthorized tool could actually change; (c) the lease's disposable git worktree anchored to `snapshot_ref`, so any unauthorized change is discarded rather than merged. NOT a compensating control: the worker's `--disallowedTools` flag — `field_failure_policy.disallowed_tools_for` is the qualification FAILURE-INJECTION harness and returns EMPTY on every clean run, so it bounds nothing in normal operation. An earlier version of this entry listed it as one; that was untruthful and is corrected here. Retirement: reconcile the two vocabularies into one, then restore the role leg. Retirement wave: Wave 2 follow-on |
| 16 | Admission check 9 (`skills_role_authorized`) is UNREACHABLE in production. Round 5 fixed the resolver to read the PERSISTED role store (`load_role_contracts()` → runtime-state `universal_work/role_contracts.jsonl`) where an operator would configure `permitted_skill_ids` / `prohibited_skill_ids`. But `persist_role_contracts` has **ZERO production callers** (only `substrate/organism/tests/test_phase11_1_universal_work.py:344`), so that store is always empty in a real deployment, the hardcoded `SEED_ROLE_CONTRACTS` fallback is always used, and **no seed carries either skill list**. Both legs of check 9 therefore pass unconditionally on every production input — the R5-F1 fix made the guard CAPABLE of enforcing but nothing populates the data it needs | one role store that production actually writes, carrying real skill bounds (owner: `substrate/organism/role_contracts.py` + whoever owns role governance) | ACTIVE_DEBT | NOT closed by deriving `permitted_skill_ids` from the archetype `responsible_role_contract_id` binding that already exists in the data: the `development` archetype — the primary Task type — stamps BOTH `software-implementation` (role-impl-op) and `verification-discipline` (role-verify-op) on a single role-impl-op Task, so enforcing the declared binding would REFUSE the most common real packet. That is the R4-3 trap (a guard that fires by refusing all real work) and it is not an acceptable fix. Reachability: check 9's refusal branch is unreachable on every production path today. Residual risk: a Task may require a skill its role does not hold — bounded by the same controls as #15 (fail-closed diff-scope verification; the disposable snapshot-anchored worktree), and by the fact that skills are advisory metadata that no worker capability is gated on today. Retirement: decide the role↔skill governance model (does a Task carry only its OWN role's skills, or the union across its lifecycle?), populate the store from that model, then the existing guard enforces with no code change. Retirement wave: Wave 2 follow-on |
| 17 | Two independently-authored ENVIRONMENT-CLASS vocabularies for one concept: planning archetypes declare `isolated_worktree` / `read_only` / `workspace` / `governed_runtime` (`substrate/execution/planning/archetypes.py:87`), while the execution side named the same git-worktree lease `git_worktree` (`decisions.py:208` default, `leases.py:57`, `placement.py:57`). Admission check 12 (`rollback_guaranteed`) accepted only `{git_worktree}`, so the two sets were DISJOINT: the check passed on every production input purely because the grant producer's DEFAULT happened to be the one literal the guard accepted — never because an archetype agreed. Any caller that forwarded the archetype's own class (the obvious wiring) would have been refused `no_rollback_guarantee` for a rollback that IS structurally guaranteed (round-6 finding R6-F3, confirmed independently in round 7) | ONE vocabulary owner: `admission.ROLLBACK_GUARANTEED_ENVIRONMENT_CLASSES` | **RESOLVED** (round 7) | Reconciled by naming BOTH spellings of the single worktree concept in one frozen set, plus `read_only` on its own merit (a zero-write environment has nothing to roll back — it is the environment the independent verifier runs in). Deliberately NOT a blanket widening: `workspace` and `governed_runtime` have no rollback mechanism and still REFUSE, as does any unrecognised class and any mixed set containing one. Pinned by `test_every_archetype_environment_class_is_adjudicated` (no archetype class may be silently unknown to admission), a 6-case parametrized guard over BOTH vocabularies, and a mixed-set test. The prior test that pinned the DISJOINTNESS as a standing property was inverted in the same change — its own failure message required updating the test and this ledger together. |
| 18 | Admission checks 8 (`role_ids`), 10 (`allowed_tools`) and 15 (`cost_limit_usd`) — numbered as the inline `── N.` section comments in `admission.py` number them — are CORRECT BUT UNDECLARED: each is strict and DOES refuse the moment its bound is set — verified by passing the bound through the REAL producer `request_execution_authorization` and observing `role_not_authorized` / `tool_not_authorized` / `unenforceable_cost_ceiling` — but the sole production caller (`transports/api/objective_plan_routes.py:426`) declares none of them, and `apply_execution_decision` has NO parameter through which an operator could. Grep for a write site of any grant bound outside `substrate/`: zero; in `cockpit/src`: zero. The module docstring previously called these "bounds the OPERATOR sets", which overclaimed (round-7 R6-F1) | an operator surface on the execution decision that carries role / tool / cost bounds (owner: Cockpit HUD execution decision, **Wave 5**) | ACTIVE_DEBT (nonblocking; reclassified HIGH -> MEDIUM) | Deliberately NOT closed by deriving the bound from the plan's own archetypes: `grant.role_ids := union(packet.required_role_contracts)` is true BY CONSTRUCTION, making check 8 a tautology AND deletable-green behind check 2 — fake reachability, which the closure order forbids. Mutation proof that the wrong fix is wrong: defaulting `role_ids` to a derived value REFUSES the `research` archetype outright (2 tests fail, incl. a live liveness break — the R4-3 trap). A bound is a control only when its authority is INDEPENDENT of the thing it bounds. Pinned in BOTH directions: `test_undeclared_bounds_do_enforce_once_declared` (enforcement half works), `test_the_production_caller_still_declares_no_operator_bound` (reads the call site SOURCE, so the day an operator surface ships this FAILS and forces reclassification), and `test_grant_bounds_are_not_derived_from_the_plans_own_packets` (anti-tautology). Not named by the Wave 2 acceptance contract, whose bounded-authorization triad is tenant / plan-version / task-set — checks 3 (tenant), 4 (plan record + version) and 17 (plan still current/approved), plus 1 (attempt-grant identity) and 2 (frontier membership), all live and mutation-proven. Retirement wave: Wave 5 |

| 19 | Wave 2 clause-5 `_check_authorization_scope` (`substrate/organism/governed_spine.py:391`) is ARMED BUT INERT on every real path. Round-9 finding B3 hardened it — an envelope carrying an `authorization_ref` used to be ADMITTED when it simply OMITTED `authorized_scope_hash` / `authorized_subject_ids`, because both comparisons were written `if env_x and grant_x and env_x != grant_x`: a MISMATCH refused, an OMISSION passed. Presence is now REQUIRED. But independent review (R9) then AST-enumerated every `ActionEnvelope`/`MutationRequest` producer in the tree: 19 production producers, only `mutation_router.py:162` forwards the five auth fields, and BOTH production `MutationRequest` producers on the Wave 2 path (`substrate/execution/intent/loop.py:250`, `transports/api/governed.py:109`) leave `authorization_ref` empty. Neither `_substrate_native_governed_mutation` nor `governed_mutation()` has an authorization parameter. No dynamic post-construction assignment exists. So the guard returns at its `if not auth_ref` early-out on every production envelope and its lookup is never invoked | an authorization-bound `ActionEnvelope` producer (owner: whoever wires HUD execution authority into the spine path, **Wave 5**) | ACTIVE_DEBT (nonblocking; NOT counted as an operational Wave 2 control) | Classified **(3) superseded by an equivalent invariant on the real path**, not (1) unwired-and-blocking: `attempts/admission.py:205-222` enforces the same two properties where dispatch is actually gated (`attempt_bound_to_grant`, `task_in_authorized_frontier`, both presence-required `bool(x) and ...`), PLUS tenant, plan+version, and a TOCTOU packet recheck the spine guard does not do; and `attempts/dispatch.py:80-86` seals `authorization_ref` / `authorized_scope_hash` / `authorized_tasks` into the package hash the worker is bound to. The Wave 2 path is therefore NOT unprotected — wiring the spine guard would add defence-in-depth, not close an open door. TWO FALSE CLAIMS were removed in the same change, both found by R9: `scripts/wave2_attempt_runner.py` asserted it "verifies each envelope's authorization is an ACTIVE grant and its scope hash matches (defense in depth over the spine's own check)" — grep found NO such check in that file — and its clause-5 comment claimed `execution_attempt_dispatch` "carries an authorization_ref", which it does not. Pinned by `test_b3_the_runner_does_not_claim_authorization_enforcement_it_lacks` and `test_b3_admission_is_the_real_authorization_authority`. Retirement wave: Wave 5 |
| 20 | `_reject_if_objective_plan_accepted` (`substrate/organism/composition_engine.py:36`) treated an UNREADABLE planning store exactly like CONFIRMED ABSENCE of a Plan (`except Exception: return`), so a store outage, corrupt record, or absent planning module silently GRANTED permission to compose a SECOND canonical Plan for an Objective that may already have an accepted one (round-9 finding B6) | `ObjectivePlanRecord` remains the sole Plan authority; "cannot tell" must never share a branch with "no plan exists" | **RESOLVED** (round 9) | Unreadable store, corrupt record, and missing planning module now all raise `CompositionAuthorityError`; only `latest is None` (confirmed absence) permits composition. Independent review (R9) confirmed HELD with no regression risk: the CALLER's `if intent.objective_id:` guard means unbound composition never reaches the function, and it verified by execution that unbound `compose_plan()` still works with the planning module absent (4 steps, no raise), 242 composition tests pass, and a genuinely corrupt store (mixed `graph_version` types) does raise — the exact case the fix targets. R9 also found the docstring still claiming the function was "tolerant of a missing planning store" AFTER the fail-closed branches landed; that stale claim is removed and pinned by `test_b6_the_docstring_does_not_claim_tolerance_the_code_lacks`. Truthful scope note: no production caller sets `objective_id` today (`CompositionIntent.objective_id` defaults to `""`, zero `objective_id=` call sites), so this hardens the seam AHEAD of its bound caller rather than fixing an actively-firing production bug. Do not claim otherwise |
| 21 | Independent review R10 found the real `start_runner` caller had ZERO behavioral regression coverage: every reference in the suite was a docstring or an `ast.FunctionDef` name comparison. An AST-SHAPE-PRESERVING mutant (keeps the call AND the value binding, corrupts only the ARGUMENT: `runner_readiness_announced("control-plane driver up: pid=%d " % proc.pid, proc.pid)`) survived all 888 Wave 2 tests while fully restoring the B1 false-readiness defect — a runner emitting only `runner starting:` was reported `started=True`. An AST pin proves a call is MADE and its value BOUND, but not that the value was computed from REAL EVIDENCE | behavioral coverage that executes the real caller against a real subprocess (`tests/test_wave2_start_runner_behavior.py`) | RESOLVED (Wave 2 R10) | 12 tests drive the REAL `start_runner` against REAL detached subprocesses; only the launched script and the bwrap preflight are substituted (structural seams the launcher already parses), never the readiness decision. Covers: control-plane marker → started; worker-only marker → started; `runner starting:` / legacy `runner up:` / another pid's readiness / pid-prefix collision / driver FATAL / instant exit / live-but-silent timeout → all fail closed. The N4b mutant is killed by 7 of these through REAL caller misbehavior (never a source-text assertion). SELF-ADVERSARIAL follow-up found TWO of my own coverage gaps that the first 12 tests missed: (a) dropping the `alive` conjunct from `if not (alive and announced)` survived — no fixture held announced=True with alive=False, so `test_announced_then_died_is_not_started` was added; and (c) an unconditional `break` in place of `if not alive: break` survived — every fixture wrote its log before the FIRST poll, so a slow-but-healthy runner would be falsely rejected, and `test_a_slow_runner_that_announces_later_is_still_accepted` was added. An initial claim that (c) was an equivalent mutant was WRONG and is corrected here. Independent review R11, rebased on the fix, then found a SEVENTH survivor on a third axis — EVIDENCE VOLUME: a truncating read (`read_text(...)[:200]`) survived all 902 tests while reporting a HEALTHY runner failed after the full deadline, because every fixture wrote only 1-3 short lines. Measured against the real runner, a MINIMAL startup with ZERO crash-recovery lines already emits 309 bytes before `control-plane driver up:` (each recovery line adds ~124), so the marker sits past any small window on EVERY real launch. Closed by `test_realistic_startup_volume_still_reaches_the_marker`, which brackets the marker with realistic chatter on BOTH sides — leading chatter kills head-window reads, trailing kills tail-window reads; one side alone leaves the other direction invisible. R11's M7 (first-occurrence-only scan) is NOT an open finding: it is covered by the sibling parametrized case 'stale then ours' in test_wave2_execution_blockers.py. R11's M8 (drop `if not alive: break`) leaves the VERDICT unchanged and only degrades latency — LOW, not a correctness defect. Final sweep: 9 readiness mutants killed across all three axes (predicate: N4b fabricated evidence, constant-pid, drop-pid-trailing-space, accept-legacy-`runner up:`; liveness: drop-alive-conjunct; deadline persistence: break-first-iteration, loop-once; volume: head-truncation, tail-truncation). A FIFTH round (self-adversarial, on the log-read axis R12 was pointed at) closed two more: an OSError branch that FABRICATES a readiness string when the log cannot be read (the N4b class exactly — a verdict invented rather than observed; closed by `test_a_failed_evidence_read_never_fabricates_readiness`, which makes the real read raise and asserts fail-closed), and decode robustness (`test_undecodable_bytes_in_the_log_do_not_crash_the_launcher`). KNOWN-UNKILLED MUTANT, recorded rather than hidden: `errors="replace"` -> `errors="strict"` is NOT killed by any test here. Three fixture attempts failed and instrumenting the launcher showed why — it decodes the log ONCE, on its first poll, so killing the mutant needs a truncated character to be the newest content at that exact instant, a sub-second race. A test that depended on winning it would be FLAKY, which is a worse defect than the low-probability crash it guards. `errors="replace"` stays as correct defensive code; the gap is declared, not papered over. Two further mutants are EQUIVALENT and correctly unkilled: `launch_log.exists()` -> `True` (the same try/except catches the resulting FileNotFoundError, so behavior is identical) and a wider tail window than the fixture's trailing chatter. Final sweep on an ISOLATED worktree: 9/9 non-equivalent mutants killed, every one by real caller behavior. THE PATTERN, recorded because it recurred five times: R9->R10 source-string vs behavior; R10->R11 predicate vs loop mechanics; R11->R12 fixture REALISM. Each round the tested seam moved and the newly load-bearing seam was untested. Fixtures must be audited against what the real runner emits on volume, ordering AND timing — not merely against the branch under test. Also fixed: R10 MEDIUM — `monkeypatch.setattr(runner, "preflight_isolation", ..., raising=False)` was a NO-OP (`hasattr` is False; `run_loop` imports it INSIDE the function from `substrate.execution.attempts.host_isolation`), so the flagship end-to-end test only reached the failure budget because bwrap happened to be installed. Now patched on the real module with `raising=` omitted so a rename fails loudly; proven load-bearing (no primitive → rc=2, preflight fails → rc=2, seam ok → rc=3). R10 LOW-1 — bound assertion `<=` → `==` (an early-firing bound at 3 cycles now FAILS; `<=` passed it silently). R10 LOW-2 — ADJUDICATED NOT A DEFECT: `737447795` changed exactly one test file and zero production `.py`, and its subject is already correctly prefixed `test:` and states the wiring "was not" tested rather than claiming a production fix; the overclaim was in surrounding prose, corrected in the PR body |
| 22 | Independent review R12 reported that three isolation-preflight mutations survived the Wave 2 suite, characterized as "production fabricates `isolation_ok=true` when the preflight crashes or returns empty/unparseable output". REPRODUCTION DISPROVED THAT CHARACTERIZATION and found a DIFFERENT, real defect in its place. Recorded here in full because the inaccurate description was briefed twice and acting on it would have produced a regression test for a behavior the shipped code never had. (a) DISPROVEN: all eleven briefed failure modes (command-not-found, nonzero-with-empty-stdout, empty stdout, whitespace-only, malformed JSON, partial/ambiguous output, stderr-only, explicit negative, and a None result) were ALREADY fail-closed at `c50f15741` — the `if pre is not None and getattr(pre, "stdout", "")` guard plus `except (ValueError, AttributeError)` covered every one; verified by executing the exact caller expression against each shape. (b) THE REAL HIGH, previously unidentified by any reviewer: the caller decided isolation SOLELY from stdout and never read `pre.returncode` or `pre.stderr`. Since `wave2_attempt_runner.py --preflight-only` returns `0 if (prim and ok) else 2`, a preflight that PRINTED an affirmative verdict and then FAILED was accepted as proof of confinement. Reproduced through the real caller BEFORE any code change: rc in {1,2,127,-9} with affirmative stdout, and rc=0 with affirmative stdout plus non-empty stderr, both returned `isolation_ok: True` AND `popen=1` — the worker process genuinely launched unconfined. That is a fail-open on the Amendment v1 clause 4 control. (c) THE COVERAGE GAP, which is what R12's mutants actually detected: the empty-stdout path was CORRECT but pinned by nothing, so `m_t2_stdout` (flip the `isolation_ok = False` initializer) survived — on any non-empty-stdout path the initializer is overwritten, making the mutant equivalent there; only the empty-stdout path distinguishes it. Correct code, unpinned. | fail-closed isolation verdict in `scripts/wave2_field_dispatch.py::start_runner` + real-path behavioral coverage in `tests/test_wave2_isolation_preflight_behavior.py` | RESOLVED (Wave 2 R12) | CORRECTION: the verdict now requires ALL of — a result object exists; `returncode == 0`; `stderr` empty; stdout non-blank; stdout parses; the parsed value is a mapping; and `isolation_ok is True` LITERALLY (a truthy string or number is malformed evidence, not proof). Every other state fails closed with a reason naming the failing condition. The strict no-stderr rule is grounded in measurement, not assumption: the real preflight on a bwrap host emits rc=0, affirmative JSON, and EXACTLY 0 bytes of stderr, so any stderr is a contradiction rather than a warning channel — and `test_real_preflight_success_emits_rc0_and_silent_stderr` pins that contract so a future chatty preflight fails loudly here instead of in the field. COVERAGE: 23 tests drive the REAL `start_runner` through the REAL `Runner.run` seam with real `CompletedProcess` objects; worker-launch is OBSERVED via the module's own `subprocess.Popen`, never inferred. `test_seam_is_load_bearing_not_an_unused_name` pins that production actually reaches the seam, so the fail-closed assertions can never pass vacuously — the exact failure mode R10's MEDIUM caught. MUTATION: 9/9 non-equivalent mutants KILLED by real caller behavior (ignore-rc, invert-rc, ignore-stderr, `m_t2_stdout`, truthy-not-literal-True, drop-empty-guard, drop-dict-guard, none-is-proven, drop-the-gate); ZERO survivors, ZERO equivalence claims required; restoration verified byte-identical (`1d6c2f6a…`) after every mutant. Two of those (drop-empty-guard, none-is-proven) initially SURVIVED and a differential over 181 reachable input shapes showed they never change `isolation_ok`, `started`, or worker-launch count — they change only WHICH cause the operator is told, misreporting an empty preflight as "not parseable JSON" and a missing result as "exited None". Rather than classify them equivalent by narrowing the contract to the verdict alone, the diagnostic was pinned (`test_refusal_names_the_condition_that_actually_failed`) and both are now killed: an operator who cannot tell a crashed preflight from a silent one cannot respond correctly, so the reason string is part of the external contract. ADJUDICATED UNREACHABLE, not silently dropped: the briefed "execution exception" and "timeout" cases cannot reach this seam as VERDICTS — `Runner.run` delegates to `subprocess.run(..., capture_output=True)` and returns a `CompletedProcess` or (CPU-gated) None; a raised timeout aborts `start_runner` entirely, which is fail-closed by construction. Adding a `try/except` to satisfy them would be dead defensive code, so the REACHABILITY CLAIM is pinned instead and fails loudly if `Runner.run` ever starts swallowing exceptions. THE PATTERN, sixth round: R11->R12 moved the axis from the readiness loop to the ISOLATION seam, and again the newly load-bearing code was the untested one — but this round also shows a reviewer's DESCRIPTION of a finding can be wrong while the finding's existence is right; the mutants were real signal, the diagnosis was not. Reproduce before correcting, and never write a regression test for a defect that reproduction cannot demonstrate  INDEPENDENT REVIEW (R14) — the first reviewer (R13) went idle twice and produced ZERO files, so its 'review' is recorded as NOT PERFORMED rather than as a clean result; an independent review that did not happen is not evidence. R14 left artifacts but never wrote its report, so its instruments were executed directly. Its driver — one module version per process, tracebacks surfaced, and a `Popen` SENTINEL that raises on worker launch — independently reproduced the defect: at the PARENT, rc=1 and rc=0+stderr both reach `PAST_GATE` (the worker-launch line, past the isolation gate), while at HEAD both return `gate_passed=False, popen=0`; crucially a HEALTHY preflight at HEAD still yields `gate_passed=True, popen=1`, so the fix is not over-strict. R14 then re-derived its own mutation set and found THREE SURVIVORS the author's 9 had missed: (a) `rc not in (0, None)` — a result carrying NO exit status treated as success; (b) dropping `.strip()` on stderr; (g) `getattr(runner, "dry_run", True)` — a runner lacking the attribute silently skips the refusal, the missing-attribute-means-do-not-enforce shape this correction exists to remove. (a) and (g) are unreachable under the CURRENT producer (`Runner.run` is typed `CompletedProcess | None`, and production `Runner` always sets `dry_run`), but unreachability is a property of today's caller, not a guarantee — so both were closed with real behavioral tests (`test_a_result_without_a_usable_returncode_fails_closed`, `test_dry_run_never_fabricates_a_real_isolation_verdict`) rather than argued away on reachability. Suite 23 -> 25. A sixteenth mutant reported NOT_APPLIED (pattern miss against a stale copy) was refreshed and applied manually rather than left unadjudicated: it KILLS (4 failures, incl. a worker launching on unparseable evidence). FINAL: 16/16 mutants killed, ZERO survivors, ZERO unadjudicated. LESSON: the author's own mutation set was systematically blind to the shapes it did not think of — every one of R14's three survivors was a missing-attribute/default-value fail-open, a class absent from the author's nine. Independent re-derivation is not a formality  CORRECTION TO THIS ROW'S OWN EARLIER CLAIM: an intermediate report of "16/16 killed, zero survivors" was WRONG and is retracted. That sweep refreshed the mutation copy MID-RUN, so early mutants executed against different source than later ones — precisely the stale-copy contamination this campaign forbids counting. Re-run against a copy proven byte-identical to HEAD BEFORE launch, the honest result was 14/16, with `r14_b_stderr_no_strip` and `r14_g_dry_run_bypass` surviving. Both were then adjudicated by execution rather than argument. `r14_b` is NOT equivalent: it is distinguishable on WHITESPACE-ONLY stderr, where real code strips and proceeds while the mutant reads "\n" as a complaint and REFUSES — a false negative that would abort a field run against a healthy bwrap host. Killed by `test_whitespace_only_stderr_is_not_a_contradiction`. `r14_g` IS a genuine EQUIVALENT MUTANT, and the reason is structural rather than a coverage gap: `start_runner` reads `runner.dry_run` a SECOND time a few lines below the mutated gate (the dry-run branch), and that access is unmutated, so a runner lacking the attribute raises AttributeError in BOTH versions with zero workers launched. Proven by differential execution over all 9 reachable runner shapes (dry_run absent/False/True x preflight rc 0/1/2): identical verdict, identical started value, identical worker-launch count, identical exception type — zero distinguishable cases. A test written for it was REMOVED as vacuous (it caught the AttributeError and returned early, so it passed against real code AND the mutant), and the equivalence proof was recorded in the test file in its place. FINAL DISPOSITION: 16 mutants — 15 non-equivalent, 15 killed by real caller behavior, 1 proven equivalent, 0 survivors, 0 unadjudicated. SECOND LESSON, on top of the first: a mutation sweep is only evidence if the source is frozen for the WHOLE sweep, and a test written to kill a survivor must be verified to actually fail against that mutant — twice here a 'fix' passed against both real and mutated code and proved nothing |

## Surface-level entries (Cockpit)

| # | Divergence | Canonical surface | Status | Migration path |
|---|---|---|---|---|
| S1 | 86 panels / 7 overlap clusters (panel audit 2026-07-20) | panel registry (`cockpit/src/renderer/panels/registry.ts`) | ACTIVE_DEBT | Wave 1 converges the planning cluster (intent/intentloop/objectiveplan → workdetail; tasks/universalwork → work; commands → chat; approvals → expanded HUD Decision surface). Remaining clusters converge in later waves |
| S2 | IntentPanel / IntentLoopPanel / CommandsPanel / TasksPanel rival surfaces | WorkDetailPanel / Work kanban / chat | ACTIVE_DEBT | C4: non-executable redirect stubs; ids never dead-link |
| S3 | Decision controls existed in chat cards | Top HUD ControlPanel ONLY | RESOLVED | Decision buttons removed from PlanSummaryCard (Wave 1 UI slice); chat surfaces/focuses the HUD item only |
| S4 | Hardcoded assistant persona names ("DEX") in some surfaces | `get_ai_name()` / SelfModel.ai_name | ACTIVE_DEBT | Touched surfaces converted in C4; full sweep tracked by vocabulary census (~92 terms, 12 collisions) |
| S5 | Operator-facing Layer-2 vocabulary (packet/intent/approval wording) across legacy panels | docs/LEXICON.md Layer 1 | ACTIVE_DEBT | Shrink-only gate `scripts/check_operator_language.py`; touched surfaces converge per commit |
| S6 | Live-WS voice session converses via `daemon.advisor.converse` directly, bypassing the planning rail | one protocol for text + voice (`try_chat_planning_rail` on `/advisor/converse`) | ACTIVE_DEBT | Voice MESSAGES and text already share the rail (platform-voice-adapter → POST /advisor/converse). Inject the rail into the live-WS converse fn in the voice follow-on |
| S7 | Execution-family rival panels (UnifiedExecution / Executor / Runtime / DistributedRuntime / ExecCoord / unwired AgentFleet) | one canonical `execution` surface | RESOLVED (Wave 2 C6) | Registry aliases `unifiedexecution/executor/distributedruntime/runtime/execcoord/agentfleet → execution`; the four rival panels are non-executable redirect stubs (pinned by `surfaceAuthority.test.tsx`); distinct diagnostics absorbed as the Execution panel's Runtime tab |
| S8 | Rival execution-decision writes at `/unified-execution/approve|reject` and `/execution/approvals/*` | `/unified-approval` ONLY (HUD) | ACTIVE_DEBT | Execution decisions surface only via `ApprovalSourceType.EXECUTION_AUTH` in the HUD; the rival panels that POSTed decisions are retired stubs. Legacy GET diagnostics remain; POST decision routes to be refused in a follow-on |
| S9 | `unifiedExecutionStore` + ExecutorPanel raw unauthenticated `fetch()` reads | canonical `executionAttemptStore` over `/execution/*` (authed `fetchApi`) | RESOLVED (Wave 2 C6) | The new store reads only the canonical execution routes via `fetchApi`; retired panels do no fetching |
| S10 | Ungoverned execution entries on operator surfaces (ExecutorPanel `/agents/run` launcher, RuntimePanel handoff input) | governed attempt path only | RESOLVED (Wave 2 C6) | Those surfaces are retired stubs; execution runs only under a governed, HUD-authorized attempt. `/fleet/*` POST fail-closed behind a decision_ref is a follow-on |

| S11 | Zero-width failure-arming window: the control plane turned a freshly-ACTIVE grant into signed dispatches on the SAME cycle, so `write-scenario-map` + `inject-failure` could never bind THIS run's grant before admission (cross-run binding reuse is correctly refused, so no workaround existed) | one run-scoped ADMISSION pause, qualification scope only | RESOLVED (Wave 2 failure-sequencing correction) | Marker `.pause_before_dispatch` beside `.inject_failure`, checked at `FieldControlPlaneDriver.run_cycle` admission next to the graph-shape gate. Bound to run_id + candidate_sha + grant_id + decision_ref and validated against the run's `execution_binding.json`, so a foreign/copied marker fails closed and cannot be released by this run. Fail-closed on every unreadable/malformed/incomplete/mismatched state; result draining is NOT suppressed (a pause can never strand a dispatched worker); a paused cycle is never reported `idle`. NOT a general workflow-pause framework: one marker, one run dir, one consumption point. **Two designs abandoned and pinned as regressions**: gating inside `_dispatch_fn` by silent return OR by raising both strand the attempt, because the scheduler transitions to DISPATCHED *before* dispatch and DISPATCHED may only go to RUNNING/FAILED/CANCELLED — never back to BLOCKED. Gating admission creates no Attempt/lease/assignment at all, so no post-dispatch rollback exists or is needed |

| S12 | Test-suite repository-root contamination: 9 test modules pinned an absolute path to the retired `c4-6-cockpit-finalization` campaign worktree, 6 of them via module-scope `os.environ.setdefault("UMH_ROOT", …)` that ran at COLLECTION time and was never restored | repo root DERIVED from the active checkout (`tests/repo_root.py`); per-test overrides only via `monkeypatch.setenv` | RESOLVED (Wave 2 validation-isolation correction) | The leak was contagious, not merely stale: `os.environ` is process-global, so every module collected afterwards inherited a foreign root. `tests/test_p1_phase2b_operator.py` reads `UMH_ROOT` at import and raised `FileNotFoundError`, producing `Interrupted: 1 error during collection` — killing an ENTIRE whole-tree shard (~127 files) while the shard still wrote a completion marker. Note the path is not deleted: the directory exists but no longer contains `substrate/`. Fixed test-only; production carries no such defect (the only non-test hits are a *detector regex* in `candidate_supply_engine.py` and a docstring line in `worktree_sandbox.py`). Order-independence proven in both directions (361 passed each way) |
| S13 | Tests read and appended to LIVE production runtime state: 10 of 11 `WorkPortfolioRuntime` constructions omit `velocity_store_path`, defaulting to `runtime_state_dir("work_portfolio")/velocity.jsonl` — the real 34k-line store production actively writes | per-test isolated runtime state (autouse `monkeypatch.setenv("UMH_ROOT", tmp_path)`) | RESOLVED (Wave 2 validation-isolation correction) | Found by fixing S12: the retired worktree had been ACCIDENTALLY SHIELDING the live tree, and deriving the root correctly exposed the real defect (the suite hung reading 34k lines, and could mutate production data). The regression invariant is deliberately CAUSAL, not a whole-file diff — production legitimately appends during test runs (observed 34,321 → 34,328 lines), so equality would be both too weak (a read still passes) and too strong (an unrelated append fails). Proven instead by resolved-path capture, a unique test-only sentinel, and an `open()` audit hook scoped to the test process. Claim is "no test-attributable live-state access or mutation", never "live state unchanged" |

| S14 | Whole-tree validation was structurally impossible: 28 test files ("Group A") constructed runtime objects that resolved into the LIVE production store at `/opt/OS/data/runtime`, and one file (`test_stage1_acceptance_e2e.py`) accumulated ~35 x 10s HTTP timeouts against an absent service. Three authoritative whole-tree shards each hit a 10,800s bound at 72% / 9% / 39% completion | manifest-scoped test-only isolation seam (`tests/runtime_isolation.py` + `tests/conftest.py`) and a bounded external-service preflight (`tests/bounded_http.py`) | RESOLVED (Wave 2 whole-tree isolation correction) | SCOPE, reconciled mechanically: 495 files scanned, 34 over the scanner bound = 1 scanner FALSE POSITIVE (`test_repo_root_isolation.py`: passes 34/34 in ~36s, over a 30s heuristic; its cost was INTRODUCED by the S12/S13 work, not pre-existing debt) + 4 FINITE-BUT-SLOW + 29 pre-existing = 28 Group A + 1 Group B. Totals verified by `diff` against the raw scanner list: EXACT MATCH, zero duplicates, zero omissions. An earlier report of "22 clean + 6 finite failures + 1 timeout = 29" was an arithmetic error caused by a `grep 'EXIT=0'` that also matched the substring inside `ERRORS=0`; the true split is 22 CLEAN_PASS + 5 FINITE_FAILURE + 1 TIMEOUT = 28, with SLOW recorded as a SECONDARY attribute on 7 files. CAUSALITY, proven by differential execution rather than inference: the SAME file at the SAME commit goes from a 60s+ timeout to `30 passed in 0.43s` when only the runtime-state root is redirected. THE SEAM redirects `UMH_STATE_DIR` (consumed at CALL time by `runtime_state_root()`, which reads it FIRST and returns before ever consulting `UMH_ROOT`, so no import-time cache defeats it) AND the working directory (the only thing that moves the RELATIVE `Path("data/runtime/...")` literals in `compute_fabric_runtime.py:441`, `distributed_runtime.py:35`, the proof dirs in `execution_authority_engine_v1.py` / `live_local_runtime_execution_v1.py` / `workpacket_execution_gate_v1.py`, and the 12-entry bootstrap list in `runtime_bootstrap_state_v1.py` — none of which pass through `runtime_state_path()`). Closing that second class with cwd is what keeps the correction TEST-ONLY; rewriting those literals would have been a production change. SCOPE IS A MANIFEST, not repo-wide autouse: a blanket fixture would silently move cwd for ~495 files whose semantics were never validated under it — a larger behavioural change than the defect. AUTHOR'S OWN REGRESSION, recorded rather than hidden: the first version ALSO overrode `UMH_ROOT`, which broke 3 tests in `test_phase14_8c_wave3.py` that legitimately resolve SOURCE artifacts (`scripts/check_*.py`, `projections/eos/`) through it — overreach past the runtime-state boundary, caught by the full-set re-run and removed; that file is back to 55 passed. STAGE-1 ROOT CAUSE CORRECTED: it is NOT an unbounded call (every request already carries `timeout=10` and swallows exceptions) but ACCUMULATION — ~35 bounded calls x 10s against a dead service ~= the 300s bound, reproduced identically on baseline `5f3c0d64c`. One bounded 3s preflight replaces it; the suite now resolves in ~4s with 50 tests SKIPPED under a reason naming the endpoint, and runs unchanged against the real runtime when the service is up. NO global network suppression (pinned by `test_no_global_network_suppression`) so unrelated defects cannot be masked. LIVE-STATE SAFETY is CAUSAL, never whole-file equality: production writes concurrently, so equality is both too weak and too strong. An `open()` audit inside the test process records zero live reads and zero live writes on completed runs. A NULL RESULT FROM A TRUNCATED PROBE IS NOT EVIDENCE — an earlier audit reported "zero access" only because a 25s cutoff killed the run BEFORE the access; that result was discarded, not reported. MEASUREMENT CORRECTION: `signal_feed.jsonl` was measured at 16,467,491,885 bytes in one window and 273,707,273 in a later one (production rotated it); the 16.5 GB figure was real when taken but is NOT a stable property and must not be quoted as one. RESULT: 28/28 files, ZERO timeouts, ~698s total (from 3 shards x 10,800s). The 5 finite failures are PRE-EXISTING and identical on baseline (`c21_3_attention_vision` 2F, `capability_catalog_slice_a`, `executive_brief_runtime`, `gate4_workstation_convergence`, `phase23_engineering_proof_loop` — the last asserting `629 != 2` because it reads real sessions, drifting to `652` at baseline, which is itself proof of live coupling). The isolated suite is therefore NOT green and is not claimed to be. MUTATION: 11/11 killed — no `UMH_STATE_DIR` and inert-manifest both killed by CAUSAL stalls (500s/400s bounds, the defect itself), no-cwd-isolation, no-cwd-restore, no-env-restore, fail-open containment, no-skeleton, no-sentinel-check, Stage-1 live HTTP, inflated default timeout, probe-always-reachable. Three were GENUINE SURVIVORS first (cwd isolation, cwd restore, env restore were pinned by nothing) and one reported survivor was EQUIVALENT (the bounded-probe test passed an explicit `timeout=`, so a mutated module DEFAULT could not reach it — closed with a separate default-value guard instead of being argued away). All mutated files verified byte-identical after every sweep. GROUP B (`test_strategic_context_runtime.py`) is deliberately NOT in the manifest: finite (1F/31P in ~99s), a deterministic key-set assertion at line 162, identical on baseline, not live-state coupled. DENOMINATOR: the committed Wave 2 set collects 1026, plus 27 new isolation tests = 1053 passed; a previously-quoted "992" was a stale figure from an earlier head, not a measurement of this tree |

## Skill classification entries

Touched Skills are inventoried during C3 archetype work as
competency | procedure | workflow | playbook | tool-instruction |
prompt-package; misclassifications land here as new rows. No full Skill
migration in Wave 1.
