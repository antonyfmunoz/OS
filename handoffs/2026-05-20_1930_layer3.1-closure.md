# Handoff — 2026-05-20 Layer 3.1 Sovereignty Cleanup Closure

## Status: CLOSED

Layer 3.1 sovereignty cleanup is complete. 9 merges across 8
sessions stripped all external IP attribution from UMH active code.

**Final merge commit**: `6dd77825` on `main`
**Canonical retrospective**: `/opt/OS/10_Wiki/LAYER_3.1_SOVEREIGNTY_CLEANUP.md`
**uvicorn**: healthy (port 8093)

## Final Tally

| Metric | Count |
|---|---|
| Manual STRIP violations closed | 191 |
| Auto-regen violations cleared | 67 |
| KEEP-AS-DATA confirmed legitimate | 32 |
| Total surface-area hits handled | ~290 |
| Original audit estimate | 67 |
| Manual-STRIP overshoot | +185% |
| Merges | 9 (d74a9149 prequel + 3.1a–3.1i) |
| Feature branches on origin | 10 (all preserved) |

## Test Baseline

**66 passed, 18 deselected**

The 18 deselected items are 4 broken classes in `tests/test_ea_final.py`:
- TestDripMatrix (4 items) — imports `runtime.drip_matrix`
- TestBuybackRate (4 items) — imports `runtime.buyback_rate`
- TestLeveragePatterns (7 items) — imports `runtime.leverage_patterns`
- TestPerfectWeek (3 items) — imports `runtime.perfect_week`

The pytest command also carries 11 `--deselect` flags targeting
class names in `test_command_surface_sync_v1.py` that do not exist
in that file — they are dead no-ops from an earlier test arrangement.
The test-hygiene merge should clean up both: fix the 4 broken classes
AND remove the 11 dead deselect flags.

## Deferred Items (Inherited from Layer 3.1)

### Test-Hygiene Merge
Fix the 4 broken test classes in test_ea_final.py (update imports
to new module paths or remove if functionality retired). Clean up
11 dead --deselect flags in pytest config.

### Archive-Hygiene Merge
Bucket D from Merge 7's full-repo grep: 37+ files across `vault/`,
`archive/`, `_archive/`, `data/umh/traces/`. Dormant data — no
runtime risk, low priority.

### Discord Command Identifiers
`!buyback`, `!drip`, `!perfectweek` — user-facing command names.
Renaming is a breaking change for users. Requires a separate
user-identifier-sovereignty decision, not a code cleanup merge.

### Architecture Questions
6 open questions in `/tmp/layer3_unified_architecture.md` (survives
as long as VPS stays up — /tmp is volatile).

### Layer 3 Phase 1 Implementation
First implementation merge: Modality + ParticipantType +
AdapterManifest as first-class types. Pending — deferred until
Antony picks next priority.

### Architecture Doc Merge
Expensive merge of `/tmp/layer3_unified_architecture.md` into
10_Wiki as canonical Layer 3 reference. Long-pending.

## Operational System Properties Locked

These are permanent learnings codified in the retrospective doc:

### Audit-Undercount Magnitudes
- File-level estimates: 3x baseline, 12.5x upper-bound
- Repo-level estimates: 2.5x baseline
- Agent-topology: can miss entire clusters (grep siblings)
- Spec-phase: 1.5x baseline (deep inspection surfaces more)
- Structural rule: audit hit counts are lower bounds, not estimates

### DATA-vs-IDENTITY Heuristic
The Leverage Principle's distinction is load-bearing, not theoretical.
32 KEEP-AS-DATA cases (world_pulse search queries, competitive_intel
tracked list, venture_knowledge ICP landscape) would have broken
core system functionality if mechanically stripped.

### Vocabulary Library
Complete and locked — all renames from Merges 1-7 documented in
Section 3 of the retrospective. New writing should check against
this table.

### Protected-File Safety Pattern
5-step reusable template for touching protected files (gateway.py,
cognitive_loop.py, model_router.py, agent_runtime.py). Documented
in Section 4 of the retrospective.

## What's NOT Next

No auto-prioritized next merge. Antony picks what's next at the
start of the next session. The deferred items above are listed
for context, not as a queue.

## Worktree State
All layer3.1* worktrees removed. Only `/opt/OS` (main) remains.
`git worktree list` returns single entry.
