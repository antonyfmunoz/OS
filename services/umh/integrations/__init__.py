"""UMH integration configuration directories.

Each subdirectory holds UMH-owned config for one integration:
manifest, transforms, and routing rules. Handler implementations
(actual API calls) live outside UMH.
"""
