# C24 Session 5: Client Auth Analysis

Generated via UMH governed development loop
Latency: 89042ms | Elapsed: 91s

et redirect result | **Remove** â€” Clerk handles internally |\n| `signInWithEmailAndPassword()` | Email/password login | `useSignIn().create({ identifier, password })` then `signIn.createdSessionId` â†’ `setActive()` |\n| `signInWithCustomToken()` | Custom token auth | **Remove** â€” Clerk uses its own session tokens |\n| `sendEmailVerification()` | Send verification email | `useUser()` â†’ `user.createEmailAddress()` or auto-verified via Clerk settings, or `user.primaryEmailAddress.prepareVerification()` |\n| `sendPasswordResetEmail()` | Password reset email | `useSignIn().create({ strategy: 'reset_password_email_code', identifier })` |\n| `verifyPasswordResetCode()` | Verify reset code | `signIn.attemptFirstFactor({ strategy: 'reset_password_email_code', code })` |\n| `confirmPasswordReset()` | Confirm new password | `signIn.resetPassword({ password })` |\n| `applyActionCode()` | Apply email verification code | `emailAddress.attemptVerification({ code })` |\n\n**Migration:** This entire file is deleted. All functionality moves to Clerk hooks and components.\n\n---\n\n### 3. `client/src/lib/authContext.tsx` â€” Auth Context Provider\n\n**Firebase functions used:**\n| Firebase Usage | Purpose | Clerk Equivalent |\n|---|---|---|\n| `onAuthStateChanged(auth, callback)` | Listen for auth changes | `useUser()` hook (reactive), `useAuth()` for `isLoaded`/`isSignedIn` |\n| `auth.signOut()` | Sign out in logout flow | `const { signOut } = useClerk()` |\n| `signInWithGoogle()` | Google login | `<SignIn>` or `useSignIn().authenticateWithRedirect({ strategy: 'oauth_google' })` |\n| `signInWithApple()` | Apple login | `<SignIn>` or `useSignIn().authenticateWithRedirect({ strategy: 'oauth_apple' })` |\n| `firebaseSignInWithEmail()` | Email login (post-register) | `useSignIn().create({ identifier, password })` |\n| `sendVerificationEmail()` | Trigger verification on register | Clerk auto-sends verification if configured |\n| `checkRedirectResult()` | Handle OAuth redirect return | **Remove** â€” Clerk handles internally |\n| `FirebaseUser` type | Firebase user object | `UserResource` from `@clerk/types` |\n| `auth.currentUser` | Current user reference | `useUser()` â†’ `user` |\n\n**Key context interface changes:**\n| Current Context | Clerk Replacement |\n|---|---|\n| `user: User` (custom) | `useUser()` â†’ `user` (Clerk `UserResource`) + your DB user via API |\n| `firebaseUser: FirebaseUser` | **Remove** â€” use `useUser()` directly |\n| `isAuthenticated: boolean` | `useAuth()` â†’ `isSignedIn` |\n| `isLoading: boolean` | `useAuth()` â†’ `isLoaded` (inverted â€” `!isLoaded` = loading) |\n| `login()` | Server-side: still call `/api/auth/login`; Clerk: `useSignIn()` |\n| `register()` | `useSignUp().create({ emailAddress, password })` + server sync |\n| `logout()` | `useClerk().signOut()` |\n| `loginWithGoogle()` | Handled by `<SignIn>` component or `useSignIn()` |\n| `loginWithApple()` | Handled by `<SignIn>` component or `useSignIn()` |\n\n**Migration:** The `AuthProvider` gets dramatically simplified. The massive OAuth redirect handling (~100 lines of retry/fallback logic) is completely eliminated. Clerk's `useUser()`/`useAuth()` replace `onAuthStateChanged`.\n\n---\n\n### 4. `client/src/pages/VerifyEmailPage.tsx`\n\n**Firebase functions used:**\n| Firebase | Clerk Equivalent |\n|---|---|\n| `applyVerificationCode(oobCode)` â€” `applyActionCode(auth, oobCode)` | `useSignUp().attemptEmailAddressVerification({ code })` or for existing users: `user.primaryEmailAddress.attemptVerification({ code })` |\n| `auth.currentUser` â€” check if user exists | `useUser()` â†’ `user` |\n| `firebaseUser.reload()` â€” refresh user data | `user.reload()` from `useUser()` |\n| `firebaseUser.emailVerified` â€” check verification status | `user.primaryEmailAddress?.verification.status === 'verified'` |\n\n**Migration:** Replace with Clerk's verification flow. Clerk uses numeric codes by default (not magic links with `oobCode`), so this page becomes a code-input form or you configure Clerk to use magic links.\n\n---\n\n### 5. `client/src/pages/ResetPasswordPage.tsx`\n\n**Firebase functions used:**\n| Firebase | Clerk Equivalent |\n|---|---|\n| `verifyPasswordResetCode(oobCode)` | `useSignIn().create({ strategy: 'reset_password_email_code', identifier })` then `signIn.attemptFirstFactor({ strategy: 'reset_password_email_code', code })` |\n| `confirmPasswordReset(oobCode, newPassword)` | `signIn.resetPassword({ password: newPassword })` |\n| `firebaseSignInWithEmail(email, newPassword)` â€” auto-login after reset | Clerk auto-creates session after `resetPassword()` â†’ `setActive({ session })` |\n| `credential.user.getIdToken()` â€” get token for server sync | `useAuth()` â†’ `getToken()` for Clerk JWT |\n\n**Migration:** Clerk's password reset is a two-step flow: (1) send code via `create()`, (2) verify code via `attemptFirstFactor()`, (3) set new password via `resetPassword()`. No `oobCode` URL params needed â€” Clerk uses email codes.\n\n---\n\n### 6. `client/src/pages/ForgotPasswordPage.tsx`\n\n**Firebase functions used:**\n| Firebase | Clerk Equivalent |\n|---|---|\n| `sendPasswordReset(email)` â€” `sendPasswordResetEmail(auth, email)` | `useSignIn().create({ strategy: 'reset_password_email_code', identifier: email })` |\n\n**Migration:** Replace the `sendPasswordReset` call. The `/api/auth/ensure-firebase-user` pre-call is also removed since Clerk manages users directly.\n\n---\n\n### 7. `client/src/pages/ProfilePage.tsx` â€” Phone Verification (2FA)\n\n**Firebase functions used:**\n| Firebase | Clerk Equivalent |\n|---|---|\n| `RecaptchaVerifier(auth, elementId, { size: 'invisible' })` | **Remove** â€” Clerk handles CAPTCHA internally |\n| `signInWithPhoneNumber(auth, phone, recaptchaVerifier)` | `user.createPhoneNumber({ phoneNumber })` then `phoneNumber.prepareVerification()` |\n| `ConfirmationResult.confirm(code)` | `phoneNumber.attemptVerification({ code })` |\n| `sendVerificationEmail()` (imported dynamically) | `user.primaryEmailAddress.prepareVerification({ strategy: 'email_code' })` |\n\n**Migration:** Clerk's phone verification is simpler â€” no RecaptchaVerifier needed. Use `useUser()` â†’ `user.createPhoneNumber()` â†’ `prepareVerification()` â†’ `attemptVerification({ code })`.\n\n---\n\n## ClerkProvider Setup\n\n```tsx\n// client/src/main.tsx\nimport { ClerkProvider } from '@clerk/clerk-react';\n\nconst PUBLISHABLE_KEY = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY;\n\nReactDOM.createRoot(document.getElementById('root')!).render(\n  <ClerkProvider publishableKey={PUBLISHABLE_KEY}>\n    <App />\n  </ClerkProvider>\n);\n```\n\n## Core Hook Replacements\n\n```tsx\n// Before (Firebase)\nimport { auth } from './firebase';\nimport { onAuthStateChanged, User } from 'firebase/auth';\n\n// After (Clerk)\nimport { useUser, useAuth, useClerk, useSignIn, useSignUp } from '@clerk/clerk-react';\n\n// Auth state\nconst { user, isLoaded } = useUser();           // replaces onAuthStateChanged + currentUser\nconst { isSignedIn, userId, getToken } = useAuth(); // replaces auth state checks\nconst { signOut } = useClerk();                  // replaces auth.signOut()\n\n// Sign in\nconst { signIn, setActive } = useSignIn();       // replaces all signInWith* functions\n\n// Sign up  \nconst { signUp, setActive } = useSignUp();       // replaces register flow\n```\n\n## Component Replacements (Optional â€” for dropping custom forms)\n\n| Custom Firebase Flow | Clerk Drop-in Component |\n|---|---|\n| Login page (email + OAuth buttons) | `<SignIn />` |\n| Register page | `<SignUp />` |\n| Forgot password + Reset password pages | `<SignIn />` with `initialValues` (built-in reset flow) |\n| Email verification page | `<SignUp />` handles verification automatically |\n| Profile page (account settings) | `<UserProfile />` or `<UserButton />` |\n\n## Files to Delete After Migration\n\n| File | Reason |\n|---|---|\n| `client/src/lib/firebase.ts` | Replaced by `<ClerkProvider>` |\n| `client/src/lib/firebaseAuth.ts` | All functions replaced by Clerk hooks |\n| `client/src/lib/authContext.tsx` | Simplified â€” `useUser()`/`useAuth()` replace entire context |\n\n## Server-Side Sync Note\n\nYour server currently receives Firebase UIDs at `/api/auth/firebase`. Post-migration, you'll send Clerk's `userId` instead. Use `useAuth().getToken()` to get a Clerk JWT, and verify it server-side with `@clerk/express` or `@clerk/backend`.\n\n```tsx\n// Client: attach Clerk token to API calls\nconst { getToken } = useAuth();\nconst token = await getToken();\nfetch('/api/auth/clerk', {\n  headers: { Authorization: `Bearer ${token}` }\n});\n```\n\n## Environment Variables\n\n| Firebase (remove) | Clerk (add) |\n|---|---|\n| `VITE_FIREBASE_API_KEY` | `VITE_CLERK_PUBLISHABLE_KEY` |\n| `VITE_FIREBASE_AUTH_DOMAIN` | â€” |\n| `VITE_FIREBASE_PROJECT_ID` | â€” |\n| `VITE_FIREBASE_APP_ID` | â€” |\n| `VITE_FIREBASE_MESSAGING_SENDER_ID` | â€” |\n| â€” | `CLERK_SECRET_KEY` (server-side) |\n\n**Note:** `usePushNotifications.ts` imports Firebase Messaging (`firebase/messaging`) â€” that's separate from auth and can remain if you keep FCM for push notifications, or migrate to a different push provider independently.","stop_reason":"end_turn","session_id":"ec23948a-e51b-4d80-b546-e1dfd750c569","total_cost_usd":0.38499824999999993,"usage":{"input_tokens":9,"cache_creation_input_tokens":36137,"cache_read_input_tokens":103544,"output_tokens":4293,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":36137,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"claude-opus-4-6":{"inputTokens":9,"outputTokens":4293,"cacheReadInputTokens":103544,"cacheCreationInputTokens":36137,"webSearchRequests":0,"costUSD":0.38499824999999993,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"fast_mode_state":"off","uuid":"99b934b3-a515-475b-a724-217225c9f7fe"}
