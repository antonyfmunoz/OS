# C24 Session 4: Server Auth Analysis

Generated via UMH governed development loop
Latency: 95377ms | Elapsed: 100s

h(), handler);\n\n// Inside handler:\nconst { userId } = getAuth(req); // userId is Clerk's user_xxx string\nconst dbUser = await storage.getUserByClerkId(userId); // you map to internal ID\n```\n\n---\n\n## `server/routes/auth.ts` â€” Route-by-Route Migration\n\n### 1. `GET /api/auth/check-email`\n- **Firebase calls:** None\n- **Clerk equivalent:** No change needed â€” this is a DB-only check\n- **Action:** Keep as-is\n\n### 2. `POST /api/auth/complete-registration`\n- **Firebase calls:** `createFirebaseUser(email, password)` â†’ stores `firebaseUid`\n- **Clerk equivalent:** **Remove entirely.** With Clerk, user registration happens on the frontend via Clerk's `<SignUp />` component or `useSignUp()` hook. The Clerk webhook (`user.created`) fires â†’ your server creates the DB user, stats, profile, integrations, daily log.\n- **Action:** Replace with a **Clerk webhook handler** at `POST /api/webhooks/clerk`. The webhook payload contains the user's Clerk ID, email, name, etc. Password hashing is Clerk's responsibility â€” remove bcrypt from registration entirely.\n\n### 3. `POST /api/auth/register`\n- **Firebase calls:** `createFirebaseUser(email, password)`\n- **Clerk equivalent:** Same as above â€” this is a duplicate registration endpoint\n- **Action:** **DELETE.** Consolidate into the Clerk webhook handler. Both `/register` and `/complete-registration` do the same thing with slight variations.\n\n### 4. `GET /api/auth/check-username`\n- **Firebase calls:** None\n- **Clerk equivalent:** No change needed â€” DB-only check\n- **Action:** Keep as-is\n\n### 5. `PATCH /api/auth/set-username`\n- **Firebase calls:** None\n- **Clerk equivalent:** `getAuth(req).userId` instead of `req.session.userId`\n- **Action:** Swap session check for Clerk auth. Optionally sync username to Clerk via `clerkClient.users.updateUser(clerkId, { username })`.\n\n### 6. `GET /api/geo/location`\n- **Firebase calls:** None\n- **Action:** Keep as-is (not auth-related)\n\n### 7. `POST /api/auth/login`\n- **Firebase calls:** None (password login uses bcrypt against DB)\n- **Clerk equivalent:** **Remove entirely.** Clerk handles login on the frontend via `<SignIn />` or `useSignIn()`. Session is set automatically.\n- **Action:** **DELETE.** The streak/health/activity logic should move to a post-login hook. Options: (a) Clerk webhook `session.created`, or (b) a `POST /api/auth/post-login` endpoint called by the frontend after Clerk sign-in completes.\n\n### 8. `POST /api/auth/sync-email-verified`\n- **Firebase calls:** `checkFirebaseEmailVerified(user.firebaseUid)`\n- **Clerk equivalent:** `clerkClient.users.getUser(clerkId)` â†’ check email verification status\n- **Action:** Replace Firebase call with Clerk API call. Or better: use the Clerk webhook `user.updated` to auto-sync when email is verified â€” eliminate polling entirely.\n\n### 9. `POST /api/auth/firebase-custom-token`\n- **Firebase calls:** `getFirebaseUserByEmail()`, `createFirebaseUser()`, `createCustomToken()`\n- **Clerk equivalent:** **No equivalent needed.** Custom tokens are a Firebase-specific bridge pattern.\n- **Action:** **DELETE entirely.** This route only exists to give the frontend a Firebase token for client-side Firebase SDK. With Clerk, the frontend uses Clerk's session directly.\n\n### 10. `POST /api/auth/ensure-firebase-user`\n- **Firebase calls:** `getFirebaseUserByEmail()`, `storage.updateUserFirebaseUid()`\n- **Clerk equivalent:** Not needed â€” Clerk is the single source of truth for user identity\n- **Action:** **DELETE entirely.**\n\n### 11. `POST /api/auth/reset-password-firebase`\n- **Firebase calls:** `verifyFirebaseIdToken(firebaseIdToken)` to authenticate the request\n- **Clerk equivalent:** Clerk handles password reset via its own `<SignIn />` forgot-password flow, or `clerkClient.users.updateUser(clerkId, { password })` on the backend\n- **Action:** **DELETE** if using Clerk's built-in reset flow. If you need a custom reset: require Clerk auth (`requireAuth()`), get `userId` from `getAuth(req)`, call `clerkClient.users.updateUser()`.\n\n### 12. `POST /api/auth/firebase`\n- **Firebase calls:** None directly, but accepts Firebase UID from the client as proof of identity (insecure â€” no server-side token verification)\n- **Clerk equivalent:** **Remove entirely.** Clerk handles Google OAuth natively. User signs in via Clerk's Google social connection â†’ `user.created` webhook fires â†’ DB user created.\n- **Action:** **DELETE.** The new user creation logic moves to the Clerk webhook. The login/streak logic moves to a post-login endpoint.\n\n### 13. `POST /api/auth/logout`\n- **Firebase calls:** None\n- **Clerk equivalent:** Clerk manages sessions. Frontend calls `clerk.signOut()`. No server-side session destroy needed unless you maintain a parallel session.\n- **Action:** **Simplify or DELETE.** If you drop express-session entirely (recommended), this becomes a no-op. The frontend just calls `clerk.signOut()`.\n\n### 14. `GET /api/auth/me`\n- **Firebase calls:** None\n- **Clerk equivalent:** `getAuth(req).userId` â†’ look up DB user\n- **Action:** Replace `req.session.userId` with `getAuth(req).userId`. Map Clerk ID to internal user.\n\n### 15. `GET /api/auth/2fa/status`\n- **Firebase calls:** `checkFirebaseEmailVerified(user.firebaseUid)`\n- **Clerk equivalent:** Clerk has built-in MFA (TOTP, SMS, backup codes). `clerkClient.users.getUser(clerkId)` â†’ `twoFactorEnabled`, email/phone verification status.\n- **Action:** Replace with Clerk's MFA APIs. Significantly simpler â€” Clerk manages the entire 2FA lifecycle.\n\n### 16. `POST /api/auth/2fa/verify-email-firebase`\n- **Firebase calls:** `checkFirebaseEmailVerified(user.firebaseUid)`\n- **Clerk equivalent:** Clerk verifies emails natively. Use webhook `user.updated` to sync, or query `clerkClient.users.getUser()`.\n- **Action:** **DELETE** or replace with a Clerk status check.\n\n### 17. `POST /api/auth/2fa/verify-phone-firebase`\n- **Firebase calls:** `verifyFirebaseIdToken(firebaseIdToken)` â†’ extracts `phone_number`\n- **Clerk equivalent:** Clerk supports phone verification natively. `clerkClient.phoneNumbers.verifyPhoneNumber()` or use Clerk's `<SignIn />` component with phone strategy.\n- **Action:** **DELETE.** Replace with Clerk's built-in phone verification.\n\n### 18. `POST /api/auth/2fa/enable` and `POST /api/auth/2fa/disable`\n- **Firebase calls:** None (DB-only toggle)\n- **Clerk equivalent:** Clerk manages MFA state. Use `clerkClient.users.updateUser(clerkId, { totpEnabled })` or Clerk's MFA enrollment APIs.\n- **Action:** Replace with Clerk MFA APIs or keep as a DB flag if you want app-level 2FA gating beyond what Clerk enforces.\n\n### 19. `GET /api/auth/google/start` and `GET /api/auth/google/callback`\n- **Firebase calls:** `getFirebaseUserByEmail()`, `createCustomToken()` (in callback)\n- **Clerk equivalent:** **Remove entirely.** Clerk handles Google OAuth end-to-end. You configure Google as a social connection in the Clerk dashboard. The frontend uses `<SignIn />` or `clerk.client.signIn.authenticateWithRedirect()`.\n- **Action:** **DELETE both routes.** The new user provisioning moves to the Clerk `user.created` webhook. Google OAuth state, token exchange, and session creation are all handled by Clerk.\n\n---\n\n## Summary: What survives, what dies, what's new\n\n### DELETE (10 routes)\n- `POST /api/auth/register`\n- `POST /api/auth/complete-registration`\n- `POST /api/auth/login`\n- `POST /api/auth/firebase`\n- `POST /api/auth/firebase-custom-token`\n- `POST /api/auth/ensure-firebase-user`\n- `POST /api/auth/reset-password-firebase`\n- `POST /api/auth/2fa/verify-phone-firebase`\n- `GET /api/auth/google/start`\n- `GET /api/auth/google/callback`\n\n### KEEP & MODIFY (6 routes)\n- `GET /api/auth/check-email` â€” no change\n- `GET /api/auth/check-username` â€” no change\n- `PATCH /api/auth/set-username` â€” swap session â†’ Clerk auth\n- `GET /api/auth/me` â€” swap session â†’ Clerk auth\n- `GET /api/auth/2fa/status` â€” replace Firebase calls with Clerk API\n- `POST /api/auth/2fa/enable` / `disable` â€” swap session â†’ Clerk auth\n\n### SIMPLIFY (2 routes)\n- `POST /api/auth/logout` â€” becomes optional/no-op\n- `POST /api/auth/sync-email-verified` / `verify-email-firebase` â€” replace with Clerk webhook or API check\n\n### NEW (2 endpoints)\n- `POST /api/webhooks/clerk` â€” handles `user.created`, `user.updated`, `session.created` events. This is where all new-user provisioning (stats, profile, integrations, daily log) moves.\n- `POST /api/auth/post-login` â€” (optional) called by frontend after Clerk sign-in to trigger streak, health, activity logging.\n\n### NEW FILE: `server/clerkAdmin.ts`\n```ts\nimport { createClerkClient } from '@clerk/express';\n\nexport const clerkClient = createClerkClient({\n  secretKey: process.env.CLERK_SECRET_KEY!,\n});\n```\n\n### DB SCHEMA CHANGE\n- Add `clerkId TEXT UNIQUE` column to users table (replaces `firebaseUid`)\n- Keep `firebaseUid` temporarily during migration, drop after all users are migrated\n\n### DEPENDENCIES\n- **Remove:** `firebase-admin`\n- **Add:** `@clerk/express`\n- **Remove (eventually):** `express-session`, `connect-pg-simple`, `bcrypt` (Clerk handles password hashing)","stop_reason":"end_turn","session_id":"0456b719-7816-4ccd-8d90-1ffcc9c1be80","total_cost_usd":0.307407,"usage":{"input_tokens":11,"cache_creation_input_tokens":26152,"cache_read_input_tokens":84204,"output_tokens":4072,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":26152,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"claude-opus-4-6":{"inputTokens":11,"outputTokens":4072,"cacheReadInputTokens":84204,"cacheCreationInputTokens":26152,"webSearchRequests":0,"costUSD":0.307407,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"fast_mode_state":"off","uuid":"e8581f0c-021c-4825-b7e2-3a02f1603d9c"}
